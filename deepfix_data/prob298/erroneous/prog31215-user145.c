#include <stdio.h>
#include <stdlib.h>
int catalan(int n) {
    if(n%2!=0)
    return 0;
    if(n==0||n==2) }
int main() {
	int i,t,k;
	scanf("%d",&t);
	for(i=0;i<t;i++) {
	    scanf("%d",&k); }
	return 0; }