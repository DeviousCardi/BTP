#include <stdio.h>
int main() {
    int i,n;
    int  A[i];
    scanf("%d",&n);
    for(i=0;i<=n;i++) {
        scanf("%d",&A[i]); }
    int x,m;
    int max;
    x=max(A[i]);
    i++;
    m=x;
    for(i=n;i>=0;i--) {
        for }
    return 0; }