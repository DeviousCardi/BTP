#include <stdio.h>
int main() {
    int n,i=0;
     scanf("%d",&n);
    int array[n];
    char arg[n];
    while(i<n) {
        while(i<n{})
        scanf("%d",&array[i]);
        i++;}
        scanf("%c",arg);
        i++; }
    i=0;
    while(i<n){
    printf("%d ",array[i]);
    i++; }
    return 0; }