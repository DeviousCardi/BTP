#include <stdio.h>
int num(char ch){
    return (ch-'0'); }
int main() {
  int n, i, j, k, flag=0;
  scanf("%d", &n);
  char arr[n];
  char a[n-1];
  for (i=0; i<n; i++) {
      scanf ("%c,", &arr[i]) }
    return 0; }