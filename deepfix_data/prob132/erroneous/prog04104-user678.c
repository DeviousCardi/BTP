#include <stdio.h>
int rec( int size,int arr[]) {
  if(size==1)
  return }
int main() {
    int n;
    scanf("%d", &n);
    int arr[n];
    for(int i=0;i<n;i++) {
        scanf("%d,", &arr[i]); }
         printf("%d,", arr[]); }