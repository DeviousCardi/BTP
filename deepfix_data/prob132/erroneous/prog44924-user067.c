#include <stdio.h>
int main() {
    int i,n,m,j;
   int count=1;
    int arr[i];
    scanf("%d",&n);
for(i=0;i<n;i++) {
    scanf("%d",&arr[i]; }
    for(i=0;i<n;i++)
    {   for(j=1;j<n;j++)
        if(ans[i]==ans[j)
        count++; }
    if(count==m) {
        printf("yes"); }
    else {
        printf("no"); }
    return 0; }