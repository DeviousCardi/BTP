#include <stdio.h>
int main() {
    int i,n,a;
   int count=0;
    int ans[i];
    scanf("%d",&n);
    for(i=0;i<n;i++) {
        if(ans[i]==a)
        count=a; }
    if(count==a) {
        printf("yes"); }
    else {
        printf("no"); }
    return 0; }