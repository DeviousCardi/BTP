#include<stdio.h>
int arr[100];
int getInversions(int l,int r) { }
int main() {
    int n,k,y;
    scanf("%d\n%d",&n,&k);
    int i,arr[n];
    for(i=0;i<n;i++){
       scanf("%d",&arr[i]); }
    y=getInversion(n,k);
    return y; }
   int getInversion(int,int) {