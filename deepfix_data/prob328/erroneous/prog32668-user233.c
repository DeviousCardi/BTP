#include <stdio.h>
#include <stdlib.h>
int fibonacci(int a) {
    if(n==0||n==1)
    return n;
    else
    return fibonacci(n-1)+fibonacci(n-2); }
int main() {
    int f=0,i,k,t;
    scanf("%d",&t);
    for(i=0;i<t;i++) {
        f=0;
        scanf("%d",&k);
        while(fibnocci(j)<=k) {
            if(fibnocci(j)==k) {
                printf("Yes");
                f=1; }
            j++; }
        if(f==0)
        printf("No"); }
	return 0; }