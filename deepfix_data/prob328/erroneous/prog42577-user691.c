#include <stdio.h>
#include <stdlib.h>
int main() {
          int fi(int n);
          return -1;
          if (n==1)
          return 0;
          if(n==2)
          return 1;
          return fi(n-1)+fi(n-2); {
            int main() {
                int i;k;t;
                scanf ("%d",&t);
                for (i=0;i<f;i++) {
                if (k<=0)
                printf("no term");
                else
                printf (%d, "fi(k)"); }
            return 0; }