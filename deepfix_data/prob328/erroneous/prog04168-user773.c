#include <stdio.h>
#include <stdlib.h>
int fibonacci(int fibo[], int n){
    if(n==0) return 0;
    if(n==1) return 1;
    return fibonacci(fibo, n-1)+fibonacci(fibo, n-2); }
int main() {
    int i;
    int fibo[20];
    fibonacci(fibo, 20);
    for(i=0; i<20, i++){
        if(n==fibo[i]);
        printf("yes");
        return 0; }
    printf("no");
	return 0; }