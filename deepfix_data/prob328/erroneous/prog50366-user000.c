#include <stdio.h>
#include <stdlib.h>
int main() {
    int t;
    int n1,n2,tmp,k=0;
    scanf("%d",&t);
    int fib(n); {
        if(n<=1)
        return n;
        else
        return fib(n-1)+fib(n-2); }
    printf("%d",n);
	return 0; }