#include<stdio.h>
#include<limits.h>
int main(){
    int n,m,i,j,w,v,r,k,e;
    scanf("%d%d",&n,&m);
    int o[m];
    for(i=0;i<=m;i++){
        int i[n];
    }for(w=0;w<=m;w++){
        for(j=0;j<=n;j++){
            scanf("%d",&ai[j]); }
    }for(i=0;i<=m;i++){
        o[i]=i[0];
        for(k=1;k<=n;k++){
            o[i]=max(o[i],i[k]); } }
    v=o[0];
    for(r=1;r<=m;r++){ }
    printf("%d",v);
    return 0; }