#include<stdio.h>
#include<limits.h>
int main() {
    int n,m,i,j ; i
    scanf("%d %d" ,&n, &m);
    int p=INT_MAX;
    for(i=0;i<n;i++) {
        int q=INT_MIN;
        for(j=0;j<m;j++) {
            scanf("%d" ,&a);
            if(a<p)
             p=a; } }
    if(a>q)
        q=a;
    printf("%d" ,q); }