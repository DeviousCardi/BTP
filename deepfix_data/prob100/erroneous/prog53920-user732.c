#include<stdio.h>
#include<limits.h>
int main() {
    int m,n;
    int i,j;
    scanf("%d%d",&n,&m);
    max=-99999
    for(i=1;i<=m;i++)
    {for(j=0;j<=n;j++) }
    int A[];
    scanf("%d",dummy);
    dummy=max ;
    max =A[j] ;
    A[j]=dummy ;
    j=j+1 ;
    scanf("%d",&A[n]);
    printf("%d",INT_MAX(&A[n]));
    return 0; }