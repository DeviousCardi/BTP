#include<stdio.h>
#include<limits.h>
int main() {
    int n,m;
    int arr[m];
    int num[n];
    scanf("%d%d",&n,&m);
    for(int i=0;i<m;i++) {
        for(int j=0;j<n;j++) {
            scanf("%d",&num[j]);
            int max=num[0];
            if(num[j]>max)
            max==num[j]; }
        num[i]==int max;
        if(max<num[0])
        printf("%d",max); }
    return 0; }