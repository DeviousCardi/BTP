#include<stdio.h>
#include<limits.h>
int main() {
    int m,n;
    scanf("%d%d",&n,&m);
    int arr[n];
    int max = arr[0];
    for(i=0;i<m;i++) {
        for(j=0;j<n;j++) {
            scanf("%d",&arr[j]); }
        if(j==n){ } }
    return 0; }