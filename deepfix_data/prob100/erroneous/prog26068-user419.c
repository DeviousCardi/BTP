#include<stdio.h>
#include<limits.h>
int rowmin(int*);
int main() {
    int n,m;
    scanf("%d\t%d",&n,&m);
    int mat[m][n];
    int i,j;
    for(i=0;i<m;i++){
        for(j=0;j<n;j++){
            scanf("%d",mat[i][j]);
            y=rowmin(mat[i][j]);
            return y; } }
    return 0; }
    int rowmin(int mat[][]) {
        for(i=0;i<m;i++){
            for(j=0;j<n;j++){
                if(mat[]) } } }