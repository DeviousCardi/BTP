#include <stdio.h>
#include <stdlib.h>
int main() {
	        int n,i,h;
	        scanf("%d",&n);
	        for(i=1;i<n;i++)
	      if((n%i==0)&&(i=n)) {
	            printf("YES"); }
	        if((n%i==0)&&(i=h)) {
	        printf("NO"); }
	return 0; }