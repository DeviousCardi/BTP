#include <stdio.h>
#include <stdlib.h>
int main() {
	int n;
	scanf("%d",&n);
	int a,b;
	if(n%a==0) { {
	      (n/a)==b; }
	   if(a+b==n) {
	   printf("YES"); }
	   else {
	       printf("NO"); } }
	return 0; }