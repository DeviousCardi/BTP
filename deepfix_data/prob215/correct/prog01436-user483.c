#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,i,sum=0;
	int s[n];
	scanf ("%d",&n);
	for (i=0;i<=n;i++) {
	s[0]=0;
	s[1]=i; {
	    for (i=0;i<n;i=i+1){
	        if(sum==s[i])
	        printf ("YES");
	        else
	        printf ("NO");
	    } } }
	return 0; }