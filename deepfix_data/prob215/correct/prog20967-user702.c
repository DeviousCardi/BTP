#include <stdio.h>
#include <stdlib.h>
int main() {
    int n;
    int l;
    int i;
    scanf("%d",&n);
    while (l==(n%i)) {
    for(i=0;i<n;i++) {
        if(l==0)
        printf("Yes");
        else
        printf("No"); } }
	return 0; }