#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,n;
    int sum=0;
    scanf("%d",&n);
    for(i=1;i<n;i++) {
         if(n%i==0)
         sum=sum+i;
         return sum; }
      if(sum=n) {
         printf("YES"); }
	return 0; }