#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,b;
	scanf("%d",&n);
	int a;
	if(n%a==0) { {
	      (n/a)==b; }
	   if(a+b==n) {
	   printf("YES"); }
	   else {
	       printf("NO"); } }
	return 0; }