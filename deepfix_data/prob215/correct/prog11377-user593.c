#include <stdio.h>
#include <stdlib.h>
int main() {
	int a,sum=0,k,x=1;
	scanf("%d",&a);
    if(a%x==0); {
    k=x;
    sum=sum+k;}
    if(a==sum)
    printf("YES");
    else
    printf("NO");
	return 0; }