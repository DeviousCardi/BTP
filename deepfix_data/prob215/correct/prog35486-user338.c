#include <stdio.h>
#include <stdlib.h>
int main() {
int n,i,j=0,count=0,sum=0;
int a[j];
scanf("%d",&n);
for(i=0;i<n/2;i++)
{for(j=0;j<=count;j++)
 {  if(n%i==0)
   {i=a[j];
       count++;}
   else continue;
   if(n==sum+a[j]);
  printf("YES"); } }
	return 0; }