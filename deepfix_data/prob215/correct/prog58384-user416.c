#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,i,j,k,m,sum=0;
	scanf("%d",&n);
	for(i=1;i<n;i++) {
	    if(n%i==0)
	    j=i;
	 for (j=1;j<n;j++) {
	         sum=sum+j;
	         sum=k; }
	  if (k>n||k<n)
	  m=1;
	  else
	  m=0;
	if(m==1)
	printf ("NO");
	else if (m==0)
	printf ("YES"); }
	return 0; }