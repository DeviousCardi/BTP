#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,n,j=0;
	scanf("%d",&n);
	int s[j];
	for(i=1;i<n;i++) {
	    if(n%i==0) {
	        s[j]=i;
	        j++;
	        continue; }
	    else {
	        continue; } }
	return 0; }