#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,i,sum;
	sum=0;
	scanf("%d",&n);
	for(i=1;i<=n-1;i++){
	   if
	   (n%i!=0)
	   continue;
	   else(
	       sum=sum+i)
	if(
	    sum==0)
	    printf("yes");
	else
	    (printf("no");)
	return 0; }