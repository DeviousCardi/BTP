#include <stdio.h>
#include <stdlib.h>
int main() {
    int sum=0,i;
    int arr[1000000000];
    for (i=0;i<n;i++)
    scanf ("%d",&arr[i]);
    for (i=1;i<n;i++) {
        if (n%i==0)
        sum=sum+i; }
    if (sum==n)
    printf ("YES");
    else
    printf ("NO");
	return 0; }