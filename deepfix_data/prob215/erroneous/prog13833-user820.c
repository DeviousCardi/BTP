#include <stdio.h>
#include <stdlib.h>
int main() {
	int n;
	scanf("%d",&n);
	int c=0;
	int a[m];
	for(m=0;m<=n-1;m++) {
	if(n%a[m]==0) {
	    scanf("%d",&a[m]);
	    c=c+a[m]; } }
	if(a[m]==n) {
	    printf("Yes"); }
	return 0; }