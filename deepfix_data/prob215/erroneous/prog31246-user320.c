#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,i,c;
	scanf("%d",&n);
	c=0;
	for(i=1;i<n;i++) {
    if(n%i==o)
    for(i=0;i<n;i++)
   { c=c+i; } }
if(c==n) {
    printf("YES");
    else
    printf("NO"); }
	return 0; }