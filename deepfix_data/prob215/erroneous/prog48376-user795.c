#include <stdio.h>
#include <stdlib.h>
int main() {
	int num,i,divisor,sum=0;
	scanf("%d",&num);
	for(i=1;i<num;i++) {
	    if("num%i==0") }
	sum=sum+divisor;
	if("num==sum") {
	    printf("Yes"); }
	else {
	    printf("No"); }
	return 0; }