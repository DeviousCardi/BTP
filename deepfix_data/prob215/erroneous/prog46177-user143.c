#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,i;
	scanf("%d",&n);
	int a,b;
	for(i=0;i<n;i++) {
	      if(n%a ==0) {
	          ((n/a)==b);
	       if(a+b==n) } {
	              printf("YES"); }
	     else {
	       printf("NO"); } }
	return 0; }