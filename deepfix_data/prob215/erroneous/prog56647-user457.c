#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,n,sum=0;
	scanf("%d",&n);
	 for(i=0;i<=n/2;i++) {
	     if (n%2==0)
	       sum=sum+i; }
	 if {
	      sum=n
	      printf("YES");
	     else
	       printf("NO");  } }
	return 0; }