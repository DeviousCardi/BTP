#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,s,count[s];
	scanf("%d",&s);
	for(i=1;i<=s;i++) {
	        scanf("%d",&count[i]); }
	if(s%i==o) {
	             count[i]; }
    else {
                 continue; }
	return 0; }