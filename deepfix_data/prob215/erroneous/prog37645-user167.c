#include <stdio.h>
#include <stdlib.h>
int main() {
    long long n;
    scanf("%ll",&n);
    int DIV(){
        count=0;
        for(i=0; i<=n; i++)
           scanf("%d",&i);
           if(n%i==0){
               return==0;
                 else
                 return==1; }
           count++;
           return 0; }
	int SUM(){ }
	return 0; }