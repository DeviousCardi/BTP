#include <stdio.h>
#include <stdlib.h>
int main() {
	int posint;
	scanf("%d", &posint);
	int i , h, r , m , a[h];
	for(i=1;i<posint;i++) {
	    if((posint/i = r)&&(posint/r = i)) {
	        for(h=0;h++) {
	            a[h] = i; } }
	    if {
	        a[h] + a[h+1] = posint;
	        m = 1; }
	    if(m == 1) {
	        printf("Yes"); }
	    else {
	        printf("No"); } }
	return 0; }