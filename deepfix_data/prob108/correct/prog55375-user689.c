#include <stdio.h>
#include <stdlib.h>
int main() {
    int n,j,k,i,l,m,a,b;
    scanf("%d",&n);
    while(n>0) {
        for(k=1;k<=i;k++); {
            if(k==1||k==i) {
                printf("*"); }
            else {
                printf(" "); } }
        printf("\n");
        i++;
        n--; }
	return 0; }