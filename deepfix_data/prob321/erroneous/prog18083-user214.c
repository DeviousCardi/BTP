#include <stdio.h>
int main() {
    int i=x1,y1,x2,y2;
        scanf("%d",&x1);
        scanf("%d",&x2);
        scanf("%d",&y1);
        scanf("%d",&y2);
    for(i=x1;X1>=100;i=i++)
        scanf("%d",&x1);
    for(i=y2;y2<=100;i=i++)
        scanf("%d",&x2);
    if m=(y2-y1)/(x2-x1);
        printf("you have the slope");
    else
        printf("error");
    if m=(y2-y1)=(x2-x1);
        printf("inf");
    else
        printf("enter value");
    return 0; }