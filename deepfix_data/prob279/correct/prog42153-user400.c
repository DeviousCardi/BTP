#include <stdio.h>
int main() {
    int n,i,j,x,y;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++) {
        scanf("%d",&n); }
    for(i=0;i<n;i++) {
        x=a[i];
        for(j=0;j<n;j++) {
            if(x=a[j])
            break; } }
    if(j!=n-1)
    printf("%d",x);
    for(i=0;i<n;i++) { }
    return 0; }