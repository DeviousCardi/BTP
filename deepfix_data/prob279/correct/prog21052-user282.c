#include <stdio.h>
int main() {
    int n,counter1=0,k,i;
    scanf("%d",&n);
    int set[k];
    char space[k];
    for(k=0;k<n;k++) {
        scanf("%d",&set[k]);
        scanf("%d",&space[k]); }
    while(set[k]==i)
    {counter1=counter1+1;}
    if(counter1==2) {
        printf("%d",i); }
    else if(counter1==0) {
        printf("%d",i); }
    return 0; }