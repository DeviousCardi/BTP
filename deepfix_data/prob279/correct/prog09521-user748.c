#include <stdio.h>
int main() {
    int n,a,i;
    int count=0;
    scanf("%d",&n);
    for(int i=1;i<=n;i++)
   count=count+1; {
          if(count!=0&&i==a) {
    printf("a\n"); }
       else
       if(i!=a) {
         printf("i"); } }
   return 0; }