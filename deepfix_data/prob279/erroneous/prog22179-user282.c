#include<stdio.h>
int main() {
    int count=0,i,j,suma,sumt;
    int difference=suma
    int a1[n];
    char a2[n];
    intt=n*(n+1)/2;
    for(k=0;k<n;k++) {
        scanf("%d",&a1[k]);
        scanf("%c",&a2[k]);
        suma=suma+a1[k] }
    for(k=0;k<n;k++) {
        scanf("%d",&a1[k]);
        scanf("%c",&a2[k]); {
            if(a1[i]=a1[j]) {
                printf("%d"\n,a1[i]);
                printf("%d"\n,difference); } }
    return 0; }