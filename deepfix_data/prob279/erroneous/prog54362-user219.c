#include <stdio.h>
int main() {
    int[n];
    int i;
    scanf("%d",&[n]);
    for(i=0;i<n;i++) {
        if (i+1==i) {
        printf("%d",i); }
        else if(i+1!=i)
        {printf("%d",i+1);} }
    return 0; }