#include <stdio.h>
int arr[100]={0};
int n;
int getMaxLessThan(int upperLimit) {
    int arr[n],i;
    for(i=0;i<n;i++) { } }
int main() {
    int n,k,c;
    scanf("%d %d",&n,&k);
    int num[n],i,j,ch;
    for(i=0;i<n;i++) {
        scanf("%d ",&num[i]); }
    for(i=0;i<n;i++) {
        c=num[i];
        for (j=i;j<n;j++) {
            if(c>num[i]) {
                ch=c; } } }
    printf("%d",ch);
    return 0; }