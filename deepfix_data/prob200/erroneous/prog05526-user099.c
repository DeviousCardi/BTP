#include <stdio.h>
int arr[100]={0};
int n;
int getMaxLessThan(int upperLimit) { }
int main() {
    int k,choco=0,i;
    scanf("%d %d\n",&n,&k);
    int(i=0;i<n;i++) scanf("%d",&arr[i]);
    for(i=0;i<k;i++) {
        choco=getMaxLessThan(choco); }
    return 0; }