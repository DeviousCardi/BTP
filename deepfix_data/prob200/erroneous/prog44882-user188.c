#include <stdio.h>
int arr[100];
int getMaxLessThan(int upperLimit) {
    int upperLimit=0;
    if(a>upperLimit)
    x=upperLimit;
    return x; }
int main() {
    int n,k;
    scanf("%d",&n);
    scanf("%d",&k)
    for(i=0;i<n;i++) {
        scanf("%d",&a[i]); }
    for(j=0;j<k;j++) {
        getMaxlessThan() }
    return 0; }