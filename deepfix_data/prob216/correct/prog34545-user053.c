#include <stdio.h>
#include <stdlib.h>
int fib(int n) {
    if(n==0)
    return 0;
    else if(n==0)
    return 1;
    else
    return fib(n-1)+fib(n-2); }
int main() {
	int k;
	int fib(int k);
	printf("%d",k);
	return 0; }