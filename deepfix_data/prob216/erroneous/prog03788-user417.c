#include <stdio.h>
#include <stdlib.h>
int search(int arrey,int key,int limit)
int main() {
    int arrey[3]={'1','3','4'}
    search(arrey[3],3,3);
	return 0; }
int search(int arrey[],int key,int n) {
    int arrey[n];
    if(key=arrey[0])
    return 0;
    if(key=arrey[n-1])
    return 1;
    else
    search(int arrey[],int key,int n-1); }