#include <stdio.h>
#include <stdlib.h>
fib(int arr,int n) {
    int i;
    arr[i]=a[i-1]+a[i-2]; }
int main() {
    int t,i;
	scanf("%d",&t);
	int n=strlen(t)
	int k[n];
	for(i=0;i<n;i++) {
	    scanf("%d",k[i]); }
	return 0; }