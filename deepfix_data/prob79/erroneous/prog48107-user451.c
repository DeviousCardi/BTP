#include <stdio.h>
#include <stdlib.h>
   char s[10];
void subset(chars1[10],int begin ,int end,int index ,int size){
    int i;
    if(index==size){
     if(size!=0){
     printf("%s",s1);
     printf("\n");}
     return ; }
    for(i=begin;i<end&&end-i+1>size-index;i++){
    data[index]=s[i];
    subset(s1,begin+1,end,index+1,size); } }
void print(int n,int size){
   char  s1[index]=s[i];
   subset(s1,0,n-1,0,size); }
int main (){
    int i,n;
    scanf("%d",&n);
    scanf("%s",s);
    for(i=0;i<n;i++);{
    print(n,i);
    printf("%s",s); }
    return 0; }
	return 0; }