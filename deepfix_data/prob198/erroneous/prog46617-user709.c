#include <stdio.h>
#include <stdlib.h>
int main() {
    int n1,n2,i,min;
    scanf("%d",&n1);
      int a[n1];
    for(i=0;i<n1;i++) {
        scanf("%d\n",&a[i]); }
     	scanf("%d",&n2);
     	int b[n2];
     	for(i=0;i<n2;i++) {
	  scanf("%d\n",&b[i]); }
      for(i=0&& min=a[0];i<n1;i++) {
          if(min>a[i])
          min=a[i]; }
      for
	return 0; }