#include <stdio.h>
#include <stdlib.h>
 int main() {
    int a,b,i;
      scanf("%d",&a);
    int c[a];
    for(i=0;i<a;i++) {
         scanf("%d",&c[i]); }
      scanf("%d",&b);
    int d[b];
    for(i=0;i<b;i++) {
          scanf("%d",&d[i]); }
    int e[a+b];
	return 0; }