#include <stdio.h>
int main()
{   int n;
    int num[n],i;
    if(n<=100 )
    for(i=0;i<n;i++)
    {scanf("%d",&num[i]); }
    for(i=0;i<n;i++)
    {printf("%d",num[i]); }
    printf("end");
    return 0; }