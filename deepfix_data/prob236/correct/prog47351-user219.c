#include <stdio.h>
int main() {
    int n;
    int num[n],i,k;
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {scanf("%d",&num[i]); }
    for(i=0;i<n;i++)
    {scanf("%d",&k); }
    printf (" %d",k);
    printf("end");
    return 0; }