#include <stdio.h>
int main() {
    int n;
    scanf("%d",&n);
    int num[n],i;
    if (n<=100)
    for(i=0;i<n;i++)
    {scanf("%d",&num[i]); }
    for(i=0;i<n;i++)
    {printf("n"); }
    printf("end");
    return 0; }