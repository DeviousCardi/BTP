#include <stdio.h>
int main() {
    int n;
    n<=100;
    int num[n],i;
    for(i=0;i<n;i++)
    {scanf("%d",&num[i]); }
    for(i=0;i<n;i++)
    {printf("%d",num[i]); }
    printf("end");
    return 0; }