#include <stdio.h>
#include<string.h>
int main() {
    int i=0;
    int n,j,k,m;
    scanf("%d\n",&n);
    int a[n];
    for(i=0;i<n;i++)
  {  scanf("%d\n",&a[i]); }
  int b[n];
  for(i=0;i<n;i++);
scanf("%d",&b[i]);
for(i=0;i<n;i++);
a[i]=b[i];
i<=n,j<=n,k<=n;
m=a[i];
if(a[j]>m) m=a[j];
if(a[k]>m) m=a[k];
for(i=0;i<n;i++)
printf("%d",a[i]);
    printf("end");
    return 0; }