#include <stdio.h>
int main(){
    int i, n;
    scanf("%d", &n);
    int book[n], order[n], temp[n];
    for(i = 0; i < n ; i++){
        scanf("%d", &book[i]); }
    for(i = 0; i < n ; i++){
        scanf("%d", &order[i]); }
    for(i = 0; i < n; i++){
        temp[b[i]] = book[i] ; }
    for(i = 0; i < n; i++){
        printf("%d ", temp[i]); }
    printf("end");
    return 0; }