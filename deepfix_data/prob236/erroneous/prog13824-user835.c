#include <stdio.h>
int main() {
    int i,j,k,n;
    scanf("%d",&n);
    for(i=0;i<n;i++) {
        scanf("%d",&a[i]);
        printf("%d",a[i]); }
    for(j=0;j<n;j++) {
        scanf("%d",k);
        printf("%d",k); }
    printf("end");
    return 0; }