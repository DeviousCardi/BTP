#include <stdio.h>
int main(){
    int n,i,book[99],order[99],book2[99];
    scanf("%d",&n);
    for(i=0;i<n;i++)
     scanf("%d",&book[i]);
    for(i=0;i<n;i++)
     scanf("%d",&order[i]);
    for(i=0;i<n;i++) {
       order[i]=num;
       book2[num]=book[i]; }
    for(i=0;i<n;i++)
     printf("%d ",book2[i]);
    printf("end");
    return 0; }