#include <stdio.h>
int main() {
    int n,m;
    int i,j,k;
    int sum[n],row[m];
    scanf("%d %d",&n,&m);
    for (i=0;i<n;i++){
for (j=1;j<=m;j++) {
int row[m];
scanf("%d ",&row[j-1]);}
for (k=0;k<m;k++) {
     sum[i]=0;
     sum[i]=sum[i]+row[k]; } }
    return 0; }