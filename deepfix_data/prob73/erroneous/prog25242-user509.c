#include <stdio.h>
int main() {
    int m,n,t,j,sum[200],f[200],s;
    scanf("%d%d",&n,&m);
    int a[200];
    for(i=1;i<=n;i++){
        for(j=0;j<=m;j++){
            scanf("%d",&f[j]);
            sum[i]=0;
            sum[i]=sum[i]+f[j];
        }}
        x=sum[i];
for(i=2;i<=m;i++){
    if(x<sum[i])
    x=sum[i];}
    printf("%d",x); }
        scanf("%d",&)
    return 0; }