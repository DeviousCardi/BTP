#include <stdio.h>
int main() {
    int n,t,p,sum=0,m;
    scanf("%d %d",&m,&n);
    int k[m*n];
    for(t=1;t<=n;t++) {
        for(p=1;p<=m;p++) {
            scanf("%d",f[t*m+p]);
            sum=k[t*m+p]+sum; }
        printf("%d",sum); } }
    return 0; }