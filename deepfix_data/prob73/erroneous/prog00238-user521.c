#include <stdio.h>
int main() {
     int n,m,i,j,a[],s[];
     scanf("%d%d",&n,&m);
     for(i=1;i<=n;i++) {
         sum=0;
         for(j=1;j<=m;j++) {
                scanf("%d",&a[j]);
             sum=sum+a[j]; }
         sum=s[i];
         printf("%d\n",sum); }
    return 0; }