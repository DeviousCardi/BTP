#include <stdio.h>
int main() {
    int n,m,i,j,h;
    j=1;
    scanf("%d %d",&n,&m);
    int t[n];
    t[n]={0};
    for(i=0;i<=n-1;i++)
        while(j<=m)
        scanf("%d",&h);
        t[i]+=h;
    return 0; }