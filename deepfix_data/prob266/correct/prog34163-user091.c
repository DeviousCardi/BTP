#include <stdio.h>
int main()
{int n,sum=0,p,i,year;
scanf("%d",&n);
for(i=0;i<n;i++) {
        scanf("%d",p);
        sum=sum+p; }
        for(year=2016;year>=2016;year++) {
            if (year%sum==0)
               break; }
        printf("%d",year);
    return 0; }