#include <stdio.h>
int main()
{int n,sum=0,p=0,i,year;
scanf("%d",&n);
for(i=0;i<n;i++) {
        scanf("%d",p);
        sum=sum+p; }
        for(year=2016;year>2016;year++) {
            if (year%sum==0)
           { printf("%d",year);
               break; } }
    return 0; }