#include <stdio.h>
int main() {
    int a,b,c,d,n;
    scanf("%d%d%d%d",&a,&b,&c,&d);
    n=(a+b+c+d);
    int i;
    for(i=(abcd);i<n;i++)
        { int j;
             j=(abcd)%n;
            if(j==0)
                    {printf("%d%d%d%d",a,b,c,d);
                        break; }
    return 0; }