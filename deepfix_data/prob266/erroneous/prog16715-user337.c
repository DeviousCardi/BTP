#include <stdio.h>
int main() {
    int y[4],i,sum=0;
    for(i=0;i<4;i++)
    scanf("%d",&y[i]);
    for(i=0;i<4;i++)
        sum++;
    while(j>2016) {
   if(j%sum==0)
   break; }
   printf("%d",j);
    return 0; }