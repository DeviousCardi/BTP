#include <stdio.h>
int main() {
    float a,c,p,x1,x2,x,y1,y2,y;
    a=x1,y1;
    c=x2,y2;
    p=x,y;
    scanf("%f%f%f%f%f%f,&x1,&x2,&x,&y1,&y2,&y");
    printf
    if(x>x1 && x<x2 && y>y1 && y<y2)
    printf("point is inside the rectangle");
    else{
        printf("point is outside the rectangle");}
        if(x>=x1 && x<=x2 && Y>=y1 && y<=y2)
        printf(point is on the line);
	return 0; }