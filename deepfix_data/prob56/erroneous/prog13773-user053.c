#include <stdio.h>
#include <stdlib.h>
int C(int n)
if(n==0)
return 1;
else return C(n+1)=((4*n+2)/(n+2))C(n);
int main() {
	int a[10];
	int i;
	int t;
	if(a[i]>k)
	scanf("%d",&t); {
	for(i=0;i<t;i++)
	{scanf("%d",a[i]);}
	a[i]=C(n); }
	printf("%d",a[i]);
	return 0; }