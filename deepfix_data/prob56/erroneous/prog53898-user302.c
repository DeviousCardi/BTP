#include <stdio.h>
#include <stdlib.h>
int brack(int n){
    if(n==0)
    return 1;
  else
  return
int main() {
int a,i,b,k;
scanf("%d",&a);
for(i=0;i<a;i++){
    scanf("%d",&b);
    for(k=0;k<100;k++){
        if(brack(k)>b){
        printf("%d",brack);
        break;}
        else{
        continue;} } }
	return 0; }