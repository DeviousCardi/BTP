#include <stdio.h>
#include <stdlib.h>
int cat(int n);
int total=0;
    if(br==0)
    return 1;
    total=total+2*n;
    br=total;
int main() {
	int i,t,k;
	scanf("%d",&t);
	for(i=0;i<t;i++) {
	    scanf("%d\n",&k);
	    if(p>k);
	    printf("%d",p); }
	return 0; }