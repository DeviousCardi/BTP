#include <stdio.h>
#include <stdlib.h>
int main() {
    int catalan(int n,int t);
    int n,i,t,k,j;
    scanf("%d",&n);
    for(i=0;i<n;i++) {
        scanf("%d",&t);
        k=catalan(j,t);
        printf("%d\n",k); }
	return 0; }
int catalan(int s,int t) {
  return 0; }