#include <stdio.h>
#include <stdlib.h>
int main() { int a[n],b[t],n,t,i,j,k,l;
scanf("%d",&n);
for(i=0;i<n;i++)
{   scanf("%d",&j)
    a[i]=j;
    if(a[i]<a[i+1])
    printf("Yes");
    else
    printf("No");
    printf("\n"); }
scanf("%d",&t);
for(k=0;k<t;k++)
{ scanf("%d",&l)
    b[k]=l;
    if(b[k]<b[k+1])
    printf("Yes");
    else
    printf("No");
    printf("\n"); }
	return 0; }