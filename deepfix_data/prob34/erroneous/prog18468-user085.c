#include <stdio.h>
#include <stdlib.h>
int main() {
int number,t,i,k;
scanf("%d",&number);
int a[number];
for(i=0;i<number;i++) {
    scanf("%d\n",&a[i]); }
scanf("%d",&t);
for(i=0;i<t;i++) {
    scanf("%d\n",j)
    if(a[j]>a[j-1] && a[j]>a[j+1])
    printf("Yes\n");
    else
    printf("No\n"); }
	return 0; }