#include <stdio.h>
#include <stdlib.h>
int main() {
	int a,b,c;
	scanf("%d %d %d",&a,&b,&c);
	if((a+b>c)&&(a+c>b)&&(b+a>c)){
	    if(c=sqrt((a*a+b*b)){
	        printf("Right Triangle");
	    }else{
	      printf("Not Right Triangle"); }
	)}else{
	    printf("Cannot form a Triangle"); }
	return 0; }