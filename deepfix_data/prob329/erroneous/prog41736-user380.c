#include <stdio.h>
#include <stdlib.h>
int main() {
	int x,y,z;
	((z>x) && (z>y));
	scanf ("%d%d%d",&x,&y,&z); {
	if !((x+y>z)&&(y+z>x)&&(z+x>y))
	printf ("Cannot form a Triangle");
	else {
	if ((x*x)+(y*y)==(z*z))
	printf ("Right Triangle");
	else
	printf ("Not Right Triangle"); }
	return 0; }