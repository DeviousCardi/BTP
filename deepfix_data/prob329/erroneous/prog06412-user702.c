#include <stdio.h>
#include <stdlib.h>
int main() {
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
    if(((a*a)+(b*b)==(c*c))||((c*c)+(a*a)==(b*b))||((c*c)+(b*b)==(a*a)))
    printf("right triangle"); }
    else
    printf("not right triangle");
    (b>a&&b>c){((a*a)+(c*c)==(b*b)) }
	return 0; }