#include <stdio.h>
#include <stdlib.h>
int main() {
	float a;b;c;
	scanf("%f,%f,%f"&a,&b,&c);
	if((a*a+b*b)>c*c);
	printf("Not Right Triangle");
	if((a*a+b*b)=c*c);
	printf("Right Triangle");
	if((a*a+b*b)<c*c);
	printf("Not Right Triangle");
	return 0; }