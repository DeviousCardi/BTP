#include <stdio.h>
#include <stdlib.h>
int main() {
int a;
int b;
int c;
scanf("%d%d%d",&a,&b,&c);
if (a+b>c || b+c>a || c+a>b)
{if (a*a = b*b+c*c || b*b = c*c+a*a || c*c =a*a + b*b)
printf("%d", right triangle);
else
printf("%d",no right triangle); }
else
printf("%d",cannot form a right triangle);
	return 0; }