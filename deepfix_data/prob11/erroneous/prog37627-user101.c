#include <stdio.h>
int sum (int a, int n){
    if (v==s){
        return v;
    else return } }
int main() {
    int n,s;
    scanf("%d %d",&n,&s);
    int a[n];
    for(i=0;i<n;i++){
        scanf("%d",&a[i]); }
    s=sum(a[0],n);
    return 0; }