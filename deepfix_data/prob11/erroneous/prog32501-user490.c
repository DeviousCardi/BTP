#include <stdio.h>
int a[1000]
int sum(int N)
int main() {
    int n,s;
    scanf("%d %d",&n,&s);
    for(int i=0;i<n;i++) {
        scanf("%d",&a[i]); }
    int b[n];
    int c=0;
    for(int j=0;j<n;j++) {
        c=sum(n); }
    return 0; }