#include <stdio.h>
int tran(int a,int b){
    int sum=0,i,j,S,N;
    for(i=0;i<N;i++) {
    for(j=0;j<N;j++) {
    if (S!=sum) {
        sum=a[j]+sum; }
    else return; }
         return; } } }
int main() {
  int S,N,i,j;
  scanf("%d %d",&N,&S);
  char str[1000000];
  scanf("%s",str);
  for(i=0;i<N;i++){
      for(j=0;j<N;j++) {
          while(str[j]!=' '){
              a[i]=str[j];
              j++; }
            i++; } }
    tran(a[0],a[1]]);
    printf("%d",tran(a[0],a[1]));
    return 0; }