#include <stdio.h>
int main() {
    int n,i,a,b;
                 int max(inta,intb);
        if(a<b)
        return a;
        else
        return b;
        scanf("%d%d\n",&a,&b);
    for(i=0;i<n;i++)
            for(i=0;i<n;i++){
                max(int(i),int(i+1));
                printf("%d"); }
    printf("end");
    return 0; }