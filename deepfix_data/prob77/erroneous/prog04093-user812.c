#include <stdio.h>
int min(int a, int b){
if (a < b)
return a;
else return b; }
int max(int b,int a);{
if(a>b)
return a;
    else return b; }
int a,b;  {
swap(a,b);
if((a,b)==(b,a))}
int main() {
    int arr[n],n,m,j,min,max;
    scanf("%d\n %d",&n,&arr[n]);
    for(i=0;i<=n;i++) {
  m=max(i,arr[n]);
   i=m;
    }printf("%d",&m); {
        for(i=0;i<=n;i++)
        M=min(i,arr[n]);
        j=M; }
    swap(m,M);
    printf("end");
    return 0; }