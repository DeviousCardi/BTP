#include <stdio.h>
int main() {
    int a,b,c;
    scanf("%d\n",&a);
    int num[a];
    for(i=0;i<a;i=i+1) {
    scanf("%d",&num[a]); }
    b==max(num[a]);
    c==min(num[a]);
    printf("end");
    return 0; }