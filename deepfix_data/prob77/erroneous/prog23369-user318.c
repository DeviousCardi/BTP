#include <stdio.h>
int max(int a,int b){
    if(a>b)return a;
    else return b;}
    int min(int a,int b){if(a<b)return a;
    else return b; }
int main() {
    int e,i,j,k,n,m;
    scanf("%d",&n);
    int arr[n];
    m=arr[0];
    p=arr[0];
    for(i=0;i<n;i++){scanf("%d",&e);
    arr[i]=a;}
    for(j=0;j<n;j++){
        m=max(arr[j],m); }
    for(k=0;k<n;k++){
        p=min(arr[k],p); }
    printf("end");
    return 0; }