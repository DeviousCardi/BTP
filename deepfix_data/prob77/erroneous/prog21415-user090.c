#include <stdio.h>
int main()
{    int k=0 ;
    intmax(inta[m],intk)
    for(i=0;i<m;i=i+1)
    if(inta[i]>k)
      return a[i];
      else
      return k ;
      k=max(a[i],k) ;
    int m,i,count;
    scanf("%d",&m);
    int a[m];
    count=0 ;
    for(i=0;i<m;i=i+1) {
        scanf("%d",&a[i]);
        printf(" %d",a[i]); }
    printf(" end");
    return 0; }