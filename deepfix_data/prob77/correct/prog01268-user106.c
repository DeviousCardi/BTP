#include <stdio.h>
int main() {
    int i,a,n,num[1000],max=0,min=0;
    scanf("%d ",&n);
    for(i=2;i<=n;i++) {
        scanf("%d",&num[i]);
        if(num[max]==i)
        max=i=a; }
    printf("end");
    return 0; }