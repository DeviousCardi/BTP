#include <stdio.h>
int main() {
    int n,a[n],j,i,M=0,m=0;
    scanf("%d",&n);
    for(i=0;i<n;i++)
        scanf("%d",&a[i]);
    for(i=0;i<n;i++) {
        printf("%d ",a[i]); }
    printf("end");
    return 0; }