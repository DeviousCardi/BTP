#include <stdio.h>
int main() {
    int i=0,n,num[1000],max,min;
    scanf("%d",&n);
    int a[i];
    for(i=0;i<n;i++) {
        scanf("%d",&num[i]);
        if(a[max]<a[i])
            max=i;
        else if(a[min]>a[i])
            min=i; }
    printf("end");
    return 0; }