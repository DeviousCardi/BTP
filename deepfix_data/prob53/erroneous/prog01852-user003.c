#include<stdio.h>
int diff(int ar[])
int main() {
    int n,i,result;
    scanf("%d",n);
    int ar[n];
    for(i=o;i<n;i++){
        scanf("%d",ar[i]); }
    result=diff(ar[n]);
    printf("%d",result);
    return 0; }
int diff(int ar[int n]){
    for(int i=0;i<n;i++){
        x=ar[0]-ar[1]
        ar[0]=x;
        ar[1]=ar[2]; } }