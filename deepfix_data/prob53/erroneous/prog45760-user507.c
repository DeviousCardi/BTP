#include<stdio.h>
int diff(int n);
int main() {
    int n,res=0;
    scanf ("%d\n",&n);
    int arr[n];
    for (i=0;i<n;i++) {
        scanf ("%d ",&arr[i]); }
    res=diff(rr[n]);
    return 0; }
int diff() {
    int i,x; }