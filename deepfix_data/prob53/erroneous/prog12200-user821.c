#include<stdio.h>
#include <math.h>
 int fun( int arr[], int size){
     int c,d;
     void swap(a,b){
     int temp;
     int a,b;
     temp=a;
     a=b;
     b=temp; }
     d=c+(a-b);
     swap(d,a);
     return (fun(arr,size-1)); }
    int main() {
        int static N ;
        int i;
        scanf("%d",&N);
        int arr[N];
        for(i=0;i<size-(size-1);i++)
        scanf("%d ",&arr[i]);
        printf("%d",return fun(arr,N));
return 0; }