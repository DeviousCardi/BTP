#include <stdio.h>
#include <stdlib.h>
int main() {
	int a[500],b[500],res[501];
	int n,m;  int i,k;
	scanf("%d%d",&n,&m);
	for(i=0;i<n;++i)
	scanf("%d",&a[i])
	for(k=0;k<m;++k)
	scanf("%d",&a[k])
	for(flag=0;;--i,--k)
	res[flag]=flag+re
	return 0; }