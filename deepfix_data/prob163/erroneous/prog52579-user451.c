#include <stdio.h>
#include <stdlib.h>
int main() {
    int  a[1000]
    int  b[1000]
    int x,y,i;
    scanf("%d %d", &x, &y);
    for(i=0;i<n;i++)
    scanf("%d %d", &a[i],&b[i]);
printf("%d",(a[i]+b[i]));
	return 0; }