#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,j,n,k,a[20],b[20];
  scanf("%d",&n);
  scanf("%d",&k);
        for(i=1;i<=n;i++)
            a[i]=0
        for(i=1;i<=n;i++)
            b[j]=0
    for(i=1;i<=n;i++)
    scanf("%d",&a[i]);
    for(j=1;j<=k;j++)
    scanf("%d",&b[j]);
    for(i=0;i<n;i++) {
    c[i]=a[n-i]+b[n-i]
    printf("%d",c[i]) }
	return 0; }