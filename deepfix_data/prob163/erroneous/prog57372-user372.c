#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int max(int a,int b){
    if (a>b) return a;
    else return b; }
int min(int a,int b){
    if (a<b) return b;
    else return a; }
int main() {
	int i,j,m,n,a1[m],a2[n];
	scanf("%d\n%d\n%d\n%d\n",&m,&n,&a1[m],&a2[n]);
	int at[max(a,b)+1];
for (i=0;i<max(m,n)-min(m,n);i++) {
    int at[i]= a1[i];}
    for (i=0;i<m;i++){
        at[i+(m-n)]=a1[i+(m-n)]+a2[i] }
	else for(i=0;i<at[i]=a2[i] }
	return 0; }