#include <stdio.h>
#include <stdlib.h>
int main() {
    scanf("%d\n",&n);
    scanf("%d\n",&m);
    char c[n],d[m];
    for(i=0;i<n;i++)
    scanf("%c",c[i]);
    printf("\n");
    for(i=0;i<m;i++)
    scanf("%c",d[i]);
	return 0; }