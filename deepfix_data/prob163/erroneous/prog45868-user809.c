#include <stdio.h>
#include <stdlib.h>
int main() {
    int a,b;
	scanf("%d%d",&a,&b);
	int x[a];
	for(i=0; i<a; i++) {
	    scanf("%d",a[i]); }
	for(j=0; j<b; j++) {
	    scanf("%d",b[i]); }
	return 0; }