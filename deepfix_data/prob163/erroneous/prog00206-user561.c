#include <stdio.h>
#include <stdlib.h>
int main() {
    int a,b,i,j;
    scanf ("%d%d",&a,&b);
    int x[a];
    int y[b];
    for (i=0;i<a;i++)
    scanf ("%d",&x[i]);
    printf x[3];
    for (j=0;j<b;j++)
    scanf ("%d",&y[i]);
    printf y[3];
	return 0; }