#include <stdio.h>
#include <stdlib.h>
int main() {
    void read_into_array_int[n];
	int n,i;
	int[n];
	for(i=0;i<n;i=i+1)
	scanf("%d",&int[i]); }
	int m,x,a,j;{
	for(j=0;j<n;j=j+1){
	    scanf("%d%d\n",&m,&x);
	    read_into_arrayint[n];
	    printf("%d%d\n",int[j],int[j]);}
	    a=((int[j])+(int[j]));{
	    printf("%d",a); }
	return 0; }