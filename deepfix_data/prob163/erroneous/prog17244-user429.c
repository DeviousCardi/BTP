#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,j,p;
	int a[500];
	int b[501];
	scanf("%d",&p);
	scanf("%d",&b); {
	for(i=0;i<=p;i++)
	printf("%d",i); }
    for(j=i;j<=i,j++)
    sum=sum+(a[i]+[b]);
    printf("%d",sum);
    return 0; }