#include <stdio.h>
#include <stdlib.h>
int main() {char i,j,a,b,sum=0;
            scanf("%c",n);
            scanf("%c",i);
            scanf("%c",j);
            for(i=0,i,n,i=i+1){
                scanf("%c"a[i]);
                scanf("%c"b[j]); }
            for(i=1;i<n;i=i+1){
                sum=a[i]+a[j]
                printf("%d\n",sum); }
	return 0; }