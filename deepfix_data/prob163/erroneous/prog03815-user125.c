#include <stdio.h>
#include <stdlib.h>
int main() {
    int n,m;
    char a[500],b[500];;
    for(i=0;i<n;i++) {
        scanf("%",x);
        a=a[x]; }
    for(j=0;j<m;j++) {
        scanf("%d",y);
        b=b[y]; }
    printf("%c",(a+b));
	return 0; }