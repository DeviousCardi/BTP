#include <stdio.h>
#include <stdlib.h>
int main() {
    char s[n];
    scanf("%c",&s[i]);
	double x,y,n,i,c;
	printf("Enter value of no of digit  x:");
	scanf("%lf",&x);
	printf("Enter value of no of digit  y:");
	scanf("%lf",&y);
	c = x + y;
	printf("sum of x,y %lf",c);
	return 0; }