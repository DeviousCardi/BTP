#include <stdio.h>
#include <stdlib.h>
int min(int x,int y) {
    int h;
    if(x<=y)
        h=x;
    if(y<=x)
        h=y;
    return h; }
int add(int a,int b) {
    int s=min(n1,n2);
    int d[s];
    int m;
    for(m=0;m<s;m++) {
        if(a+b<10)
            d[m]=a+b;
        else
            d[m]=(a+b)%10; } }
int main() {
    int i,j,k,n1,n2;
    int a[n1],b[n2];
    scanf("%d\n",&n1);
    scanf("%d\n",&n2);
    for(i=n1-1;i>=0;i--) {
        scanf("%d",&a[i]); }
    for(j=n2;j>=0;j--) {
        scanf("%d",&b[j]); }
    for()
	return 0; }