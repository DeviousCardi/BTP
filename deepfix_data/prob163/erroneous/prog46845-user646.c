#include <stdio.h>
#include <stdlib.h>
int main() {
	int a[n],i;
	int long(int n);
	int long (int sum);
	scanf("%ld",&n);
	for(i=0;i<n,i++)
	scanf("%ld",a[i]);
	sum=a[n-1]+a[n-2];
	printf("%ld",sum);
	return 0; }