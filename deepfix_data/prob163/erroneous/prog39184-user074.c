#include <stdio.h>
#include <stdlib.h>
int main() {
int i,j,k,n;
char a[501],b[501],d[502];
scanf("%d %d",&n,&k);
    scanf("%s",a[]);
    scanf("%s",b[]);
for(i=0;i<=500;i++) {
        if(a[i]+b[i]/10!=)
        d[i]=a[i]+b[i]+1;
        else
        d[i]=a[i]+b[i]%10; }
    printf("%c",d[i]);
	return 0; }