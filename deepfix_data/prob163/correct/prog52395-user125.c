#include <stdio.h>
#include <stdlib.h>
int main() {
    int n,m,i,j,x,y;
    int a[500],b[500],c,d;
    for(i=0;i<n;i++) {
        scanf("%d",x);
        c=a[x]; }
    for(j=0;j<m;j++) {
        scanf("%d",y);
        d=b[y]; }
    printf("%d",(c+d));
	return 0; }