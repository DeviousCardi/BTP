#include <stdio.h>
#include <stdlib.h>
int main() {
	int v=0,n=0,i,c;
	scanf ("%d",&c);
	scanf("%d",&c);
long long int a[n];
	for (i=0;i<=n;i++)
	scanf("%lld",&a[n]);
	while (v<1);
	  {  v=a[n]/10; }
	    c=c+1;
long long int sum=0;
	sum= a[c]+a[c];
	printf ("%lld",sum);
	return 0; }