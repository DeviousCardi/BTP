#include <stdio.h>
#include <stdlib.h>
int main() {
	int  n=0,i,c,d;
	scanf ("%d\n",&c);
	scanf("%d\n",&d);
long long int a[n];
	for (i=0;i<=n;i++) {
    scanf("%lld",&a[n]); }
	int p=0,k=0;
	scanf ("lld\n",&a[p]);
	scanf ("lld\n",&a[k]);
int v=a[n];
while (v<1);
	  {  v=v/10; }
	    c=c+1;
long long int sum;
	sum= a[p]+a[k];
	printf ("%lld",sum);
	return 0; }