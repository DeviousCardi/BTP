#include <stdio.h>
#include <stdlib.h>
int main() {
	int v=0,n=0,i,c;
	scanf ("%d",&c);
	scanf("%d",&c);
long long int a[n];
	for (i=0;i<=n;i++)
	scanf("%lld",&a[n]);
	while (v<1);
	  {  v=a[n]/10;
	    c++; }
long long int sum=0;
int p,k;
	sum= a[p]+a[k];
	printf ("%d",sum);
	return 0; }