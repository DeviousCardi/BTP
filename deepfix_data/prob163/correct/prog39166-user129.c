#include <stdio.h>
#include <stdlib.h>
int main() {
	int v,n,i,c;
	scanf ("%d",&c);
	scanf("%d",&c);
long long int a[n];
	for (i=0;i<=n;i++)
	scanf("%lld",&a[n]);
	while (v<1);
	  {  v=a[n]/10;
	    c++; }
	return 0; }