#include <stdio.h>
#include <stdlib.h>
int main() {int l,p,i,j,n;
            int a[i];
            int b[i];
            scanf("%d",&l);
            scanf("%d",&p);
            for(i=1;i<n;i=i+1){
                scanf("%d",&a[i]);
                scanf("%d",&b[i]); }
            for(i=1;i<n;i=i+1){
                p=a[i]+b[i];
                printf("%d\n",p); }
	return 0; }