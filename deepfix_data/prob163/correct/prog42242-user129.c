#include <stdio.h>
#include <stdlib.h>
int main() {
	int v=0,n=0,i,c;
	scanf ("%d\n",&c);
	scanf("%d\n",&c);
long long int a[n];
	for (i=0;i<=n;i++) {
    scanf("%lld",&a[n]); }
	int p,k;
	scanf ("lld\n",&a[p]);
	scanf ("lld\n",&a[k]);
	while (v<1);
	  {  v=a[n]/10; }
	    c=c+1;
long long int sum;
	sum= a[p]+a[k];
	printf ("%lld",sum);
	return 0; }