#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,j,p,sum,v;
	int a[500];
	int b[501];
	scanf("%d",&p);
	scanf("%d",&v); {
	for(i=0;i<=p;i++)
	printf("%d",i); }
    for(j=i;j<=i;j++)
    sum=sum+((a[i]+b[j]));
    printf("%d",sum);
    return 0; }