#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,j,a,b;
	char m,n,d,k;
	scanf("%c",&m);
	int arr[m];
    for(i=0;i<m;i++){
     scanf("%d",&arr[i]);
    for(j=i;j<m;j++){
     scanf("%c",&d); } }
  { scanf("%c",&n);
   int arr[n];
   for (a=0;a<n;a++){
    scanf("%d",&arr[a]);
    for(b=a;b<n;b++){
        scanf("%c",&k); } }
    printf("%c",d+k);
}	return 0; }