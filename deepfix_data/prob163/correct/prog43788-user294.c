#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,n;
    int s[n];
    scanf("%d",&s[i]);
	 int j,a;
    int t[a];
    scanf("%d",&t[j]);
	double x,y,c;
	printf("Enter value of no of digit  x:");
	scanf("%lf",&x);
	printf("Enter value of no of digit  y:");
	scanf("%lf",&y);
	c = x + y;
	printf("sum of x,y: %lf",c);
	return 0; }