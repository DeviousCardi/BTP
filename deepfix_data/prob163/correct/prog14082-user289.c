#include <stdio.h>
#include <stdlib.h>
int main() {
    int n,m,i;
    scanf("%d",&n);
    scanf("%d",&m);
    int a[600];
	char c;
	c=getchar();
	for(i=0;i<n;i++) {
    a[i]=c;
	c=getchar(); }
	return 0; }