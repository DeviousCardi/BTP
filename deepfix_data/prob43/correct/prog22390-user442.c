#include <stdio.h>
#include <stdlib.h>
int mod(int, int);
int fxn(int);
int gxn(int);
int main() {
    double a,b, sum=0, x;
    int n, i, count=0;
    scanf("%lf %lf %d",&a,&b,&n);
	x=(b-a)/n;
	return 0; }