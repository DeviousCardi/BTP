#include <stdio.h>
#include <stdlib.h>
int main() {
    double a,b,area;
    int i, n,sum=0;
    scanf("%lf%lf%d",&a,&b,&n);
for(i=0;i<=2;i++)
    sum==sum+8*i-2*i*i*i;
sum=6;
for(i=3;i<=n-1;i++)
    sum==sum+2*i*i*i-8*i;
    area==sum*((b-a)/n);
    printf("%.4lf",area);
	return 0; }