#include <stdio.h>
#include <stdlib.h>
# include <math.h>
int main() {
	float x,f,g,n,x1,x2;
	f=(((-1)*(x*x))+(3*x));
	g=((2*x*x*x)-(x*x)-(5*x));
	x>=-2,&x<=2;
	scanf("%f",&x1,&x2,&n);
	x1>=-2,&x2<=2;
  if (x>=-2,&x<=0){
      float a,n,m,j;
      a=(f-g);
     m=2/n;
     j=a*m;
     printf("%f",j); }
  if(x>=0,&x<=2){
      float b,p,k,n;
      b=(g-f);
      p=2/n;
      k=b*p;
      printf("%f",k); }
	return 0; }