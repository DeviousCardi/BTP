#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int main() {
    int n,i,j;
    double a,b,s1,s2,s,x;
    scanf("%lf%lf%d",&a,&b,&n);
       abs (s1=2*x*x*x-8*x);
       for(i=0;i<n-1;i++) {
      for(j=2;j>i-2;j++) {
      if(a==-2&&b==0)
      printf("s1");
      else
      printf("s2");}
     s=abs(s1)+abs(s2);
    printf("%.4lf",s);
    return 0; } }