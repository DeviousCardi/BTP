#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,i;
	float a,b,x,k[i],sum=0.0,area;
	scanf("%d",&n);
	for(i=0;i<=n-1;i++) {
        x=(double)(b-a)/n;
	    scanf("%lf",&k[i]);
	    k[i]=(8*i-2*i*i*i);
	    if(k[i]>=0)
	    k[i]=k[i];
	    else
	    k[i]=-k[i]; {
	        sum=sum+k[i]; }
	    area=sum*x;
	    printf("%lf",area); }
	return 0; }