#include <stdio.h>
#include <stdlib.h>
#include <math.h>
double f(double a){
    double r= -a*a + 3*a;
    return r; }
double g(double x){
double r= 2*x*x*x -x*x -5*x;
return r;}
int main() {
int n,i;double a,b,sum=0.0;
printf("%lf",f(2));
	return 0; }