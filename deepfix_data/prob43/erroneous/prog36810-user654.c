#include <stdio.h>
#include <stdlib.h>
int main() {
	      int n,z,x,f,g;
	      double a,b;
	      scanf("%lf%lf%d",&a,&b,&n);
	      for (i=0;i=n-1;i++) {
	        printf("%.4lf",area); }
	      f(x)=-x*x+3*x;
	      g(x)=2*x*x*x*-x*x-5*x;
	      z=(b-a)/n;
	      area=(f(x*i)-g(x*i))*z;
	return 0; }