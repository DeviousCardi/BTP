#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
	               double  q=0,w=0,s,ar,a,b,x,z;
	               int     i,n;
	               scanf("%lf%lf%d",&a,&b,&n);
	               ar=0;
	               for (i=0;i<=(n-1);i++){
	                   x=(b-a)/n;
	                   q= -pow(z,2) + zx;
	                   w= 2*pow(z,3)-pow(z,2)-5z;
	                   s= ((q*i)-(w*i))*x;
	                   ar= ar + s;
	                   printf("0.4%lf",ar); }
	return 0; }