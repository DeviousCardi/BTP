#include <stdio.h>
#include <stdlib.h>
int main() {
	double a;
	double b;
	int n;
	b>=a;
	scanf("%lf%lf",&a&b);
	scanf("%d",&n);
	int i;
	double x;
	double fxi=-(xi*xi)+3*xi;
	double gxi=(2*xi*xi*xi)-(xi*xi)-(5*xi);\
	double sum=0.0;
	for(i=0;i<n;i++){
	    if(fxi>gxi){
	        sum=sum+(fxi-gxi)*((b-a)/n) }
	    else
	    sum=sum+(gxi-fxi)*((b-a)/n) }
	printf(%.4lf,sum);
	return 0; }