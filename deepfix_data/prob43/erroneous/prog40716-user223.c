#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
	       float a,b;
	       int n,i;
	       scanf("%f",&a);
	       scanf("%f",&b);
	       scanf("%d",&n);
	       float area=0.0;
	       int p=((b-a)/n);
	       for(i=0;i<n;i++)
	       { for (z>=a;z<=b;z++)
	          ( area=area+(abs((3*z)+(5*z)-(2*pow(z,3))*(p)))); } }
	       printf("%.4f",area);
	return 0; }