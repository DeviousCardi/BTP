#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
double a,b,h;
int n;
int x[n];
float y;
double h(float y) {
    if h(y)>= 0
return h(y);
else
return -h(y); }
double sum = 0 ;
scanf("%lf%lf%d";&a,&b,&n);
for (i=0;i<n;i++)
{x[i] = a + (b-a)/n*i};
for (i=0;i<n;i++) {
    sum = sum + h(-2x[i]*x[i]*x[i] + 8x[i]) ; }
printf("%.4lf",sum);
	return 0; }