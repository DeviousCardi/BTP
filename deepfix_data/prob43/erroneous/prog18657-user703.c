#include <stdio.h>
#include <stdlib.h>
#include <math.h>
const double epsilon=0.000001;
int a1[100000];
void part(double a,double b,int n ){
    int i=0;
    for(i=0;i<=n;i++){
        a1[i]=a+i*(b-a)/n;} }
double f(double x,int i){
    double d= abs(2*a1[i]*a[i]*a[i]-8*a[i]);
return d; }
int main() {
	double a,b,sum=0;
	int n;
	int i;
	scanf("%lf",&a);
	scanf("%lf",&b);
	scanf("%d",&n);
	void part()
	return 0; }