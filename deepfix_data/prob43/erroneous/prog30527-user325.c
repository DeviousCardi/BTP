#include <stdio.h>
#include <stdlib.h>
int ab.(n){
    	int n,r;
	b=abs(n);
	return b; }
int main() {
    double x,a,b,c,d;
    int n,i,r;
    scanf("%lf%lf%d",&a,&b,&n);
    double l[n];
    for(r=0;r<=n;r++) {
        l[r]=a+r*((b-a)/n); }
    x=(b-a)/n;
    for(i=0;i<=n-1;i++) {
        c=-2*(l[i]*l[i]*l[i])+8*l[i];
        d=(ab.(c))*x; }
	return 0; }