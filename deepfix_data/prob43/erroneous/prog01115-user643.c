#include <stdio.h>
#include <stdlib.h>
int main() {
	double  a,b,n;
	double i,c;
	scanf("%lf",&n);
	double sum=0,area;
	double x,y;
	double k=(b-a)/n;
	double m(double x){
	  m=(-x*x+3*x)-(2*x*x*x-x*x-5*x); }
	double abs(double y){
	    if (y>0){abs=y;}
	    if (y<0){abs=-y;}
	    if (y=0){abs=0;}
	}return double
	for(i=0;i<=n-1;i++){
	    c=a+k*i;
	    m(c)=abs(m)*k;
	    sum=sum+m(c);
	    area=sum; }
	   printf("%lf";area);
	return 0; }