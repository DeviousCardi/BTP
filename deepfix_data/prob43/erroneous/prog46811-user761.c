#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int main() {
    int n,x,i;
    double a,b;
    scanf("%d%d%d",&n,&a,&b);
f(x)=-(x^2)+3*x ;
g(x)=2*(x^3)-x^2-5*x ;
   area=sum
printf("%0.4",f);
	return 0; }