#include <stdio.h>
#include <stdlib.h>
int main() {
    int n,x,i;
    float double a,b,t,z;
    float s=0.0
    char f,g;
    f=-x*x+3*x;
    g=2*x*x*x-x*x-5*x;
    t=(b-a)/n;
    z=abs(f-g)*t;
    for(i=0;i<=n;i++) {
        s=s+z; }
    printf("%lf",s);
	return 0; }