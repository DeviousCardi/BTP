#include <stdio.h>
#include <stdlib.h>
int mod(int n){
    if(int a>=o){
        mod(a)=a; }
    else{
        mod(a)=-a; }
    return a; }
int f(int x){
   int value=-x*x+3*x;
       return value; }
int g(int x){
    int value=2*x*x*x-x*x-5*x;
    return value; }
int main() {
    double n;
    double a,b;
    int value;
    double sum=0;
    scanf("%d",n);
    for(i=0;i<n;i++){
        value=mod(f(a+i(b-a)/n)-g(a+i(b-a)/n))*(b-a)/n;
        sum=sum+value; }
	printf("%lf",sum)
	return 0; }