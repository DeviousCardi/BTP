#include <stdio.h>
#include <stdlib.h>
const double epsilon = 0.000001;
int modulus(int a)   {
    int i,j;
    if((i-j)>0)   return (i-j);
    if((i-j)<0)   return (j-i); }
double fx(double x)   {
    double i;
    for(i=-2; i<=2; i=i+(b-a)/n) }
int main() {
    double a,b;
    scanf("%lf",&a);
    scanf("%lf",&b);
    int n;
    scanf("%d",&n);
    double area;
    double term;
    double interval = (b-a)/n;
    for(i=1; i<=n; i++)    { }
	return 0; }