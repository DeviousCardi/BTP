#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
	double a,b,c;
	int n,sum;
	sum=0
	c=(b-a)/n;
	scanf("%lf%lf%d",a,b,n);
	f(x)=(-x*x)+(3*x);
    g(x)=(2*x*x*x)-(x*x)-(5*x);
	for(i=0;i<n;i++) {
	scanf("%lf",&x[i]);
	    for(i=0;i<n;i++) {
	        sum=sum+(f(x[i])-g(x[i]))*c }
	  sum=sum }
	return 0; }