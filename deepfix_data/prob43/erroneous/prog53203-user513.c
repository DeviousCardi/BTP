#include <stdio.h>
#include <stdlib.h>
int main() {
	int n;
	double a,b,x,area,p;
	p= -2(x*x*x)+ 8*x;
	scanf("%d%lf%lf",&n,&a,&b);
	if (p>=0) {
	            area=f*((b-a)n);
	            printf("%.4lf",area); }
	else {
	        area=-p*((b-a)/n);
	        printf("%.4lf",arae); }
	return 0; }