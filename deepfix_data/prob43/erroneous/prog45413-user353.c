#include <stdio.h>
#include <stdlib.h>
int main() {
    void_fun(void);
    def_fun(int x);
    print_area(float a,float b,int n);
	{int i,n;
	float a,b,g;
	scanf("%f%f",&a,&b);
	printf("%f",area);
	def_fun(int x); {
	f(x)==-x*x+3*x;
	g(x)==2*x*x*x-x*x-5*x;
	return(fun); }
	print_area(float a,float b,int n);{
	    return (abs(y); }
    return(void);
    cal_area(float a,float b,int n);
     sum=0;a==a[0];b==a[n],area=0;
     g==(b-a)/n
     for(i=0;i<n;i++){
        scanf("%f",&a[i]);
        area=(abs((f(a[i]))-(g(a[i])))*g
        sum=sum+area;
        i++;
        return (area)}
	return 0; }