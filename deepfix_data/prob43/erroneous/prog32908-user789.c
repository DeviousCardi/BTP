#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
	double a,b,w,k,s,s1,s2,area=0;
	int n,i;
	scanf("%l%l%d",&a,&b,&n);
	w=(b-a)/n;
    k=(-a/(b-a))*n;
	for(i=1;i<=n;i++) {
	        x=a+w*i;
	        if(a,b=>-2&&a,b<0)
	        s=(2*pow(x,3)-8*x)*w;
	        area=area+s;
	        else if((a=>-2&&a<=0)&&(b=>0&&b<=2))
	        for(i=1;i<k;i++) {
	            s1=(2*pow(x,3)-8*x)*w; }
	        for(i=k;i<=n;i++) {
	          s2=(8*x-2*pow(x,3))*w; }
	        area=area+s1+s2;
	        else if(a,b=>0&&a,b<=2)
	        s=(8*x-2*pow(x,3))*w;
	        area=area+s; }
	    printf("%.4l",area);
	return 0; }