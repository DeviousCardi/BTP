#include <stdio.h>
#include <stdlib.h>
double f (double z) {
    ans=(-z*z)+(3*z);
    return ans; }
double f (double z) {
    ans=(2*z*z*z)-(z*z)-(5*z);
    return ans; }
int main() {
    double a,b;
    int n;
    scanf("%lf",&a);
    scanf("%lf",&b);
    scanf("%d",&n);
	return 0; }