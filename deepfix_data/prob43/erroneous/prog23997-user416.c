#include <stdio.h>
#include <stdlib.h>
int main() {
	int n;
	double a,b,dx,areai;
	for(i=0;i<=n-1,i++) {
	    dx=(b-a)/n;
	    k=(8*i-2*i*i*i);
	    while (k>=0)
	    k=k;
	    else
	    k=-k;
	return 0; }