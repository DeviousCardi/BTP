#include <stdio.h>
#include <stdlib.h>
       float mod(x)
       if (x>=0)
       return x;
       else
       return -x;
    main() {
           int n,i,b,x[n];
           float a,A=0;
           scanf("%d"&n);
           scanf("%d"&b);
           scanf("%f"&a);
           for(i=0;i<=n-1,i++){
               A=(mod(8*x[i]-2*x[i]^3))*(b-a)/n;
               printf; }
return 0; }