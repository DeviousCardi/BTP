#include <stdio.h>
#include <stdlib.h>
int h(float x){
    r= (8*x) - (2*x*x*x);
    return r; }
int sum(h(x)){
    for (i=0;i<=n-1;i++){ } }
int main() {
	        int n;
	        double a,b,s,area;
	        scanf("%lf%lf",&a&b);
	        scanf("%d",&n);
	        s= (b-a)/n;
	        area = sum( * s;
	return 0; }