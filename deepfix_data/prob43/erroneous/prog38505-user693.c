#include <stdio.h>
#include <stdlib.h>
double f(x)=-x*x+3*x; {
    f(x)=-x*x+3*x;
    return 0; }
double g(x)(double) {
    g(x)=2*x*x*x-x*x-5*x;
    return 0; }
int main() {
    int n;
    double a,b;
    scanf("%lf%lf",&a,&b);
	return 0; }