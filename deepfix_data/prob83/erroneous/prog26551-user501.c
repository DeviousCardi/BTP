#include <stdio.h>
int rotate(int ar[],int d);
    {return k;}
int main() {
    int n,i,d,ar[1000];
    scanf("%d\n",&n);
    for(i=0;i<n;i++)
        scanf("%d",&ar[n]);
    printf("\n");
    scanf("%d",&d);
    int rotate(int ar[],int d);
    {int i,n,k;
        for(i=0;i<n;i++)
            {k=ar[i];
             ar[i+d]=k;
             return k;}}
    printf("%d",rotate(n,d));
    return 0; }