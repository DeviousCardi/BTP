#include <stdio.h>
int main() {
  int i,n;
  scanf("%d",&n);
  i=n;
  int d;
  scanf("%d",&n)
  int a[n];
  int rot(int a[n] , int d);
  while(i--) {
      if((i-d)>0)
      a[i]=a[i-d];
      else
      a[i]=a[n+i-d] }
    return 0; }