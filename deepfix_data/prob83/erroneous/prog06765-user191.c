#include <stdio.h>
void print(int a)
{printf("%d",a); }
int main()
{   int n,d,i,j;
    scanf("%d",&n)
    int a[n];
    for(i=o;i<n;i++)
    {   scanf("%d",&a[i]) }
    scanf("%d",&d);
    for(j=0,i=n-d;j<n;j++)
    {   void print(a[i]);
        i=i+1;
        if(i==n)
        i=0; }
    return 0; }