#include <stdio.h>
int main() {
    int n,i,b,k,m;
    int j=400;
        scanf("%d/n",&n);
    int a[2*n];
     {for(i=1;i<=2*n;i=i+1);}; {
            scanf("%d",&a[i]); }
     {for(k=1;k<2*n;k=k+1);}; {
            for(m>k;m<2*n;m=m+1);
                if(a[k]==a[m])
                    {b=m-k;
                    };
                if (b<j) {
                    j=b; }
                        printf("%d",&j); }
    return 0; }