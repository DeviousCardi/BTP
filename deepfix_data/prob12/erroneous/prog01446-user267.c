#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int s[2*n];
    int i,y;
    for(i=0;i<2*n;i++) {
        scanf("%d",&y);
        s[i]=y; }
int h[n];
int r,o;
for(r=1;s[o]!=r;r++){
    return 0; }