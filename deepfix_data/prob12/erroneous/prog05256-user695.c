#include <stdio.h>
int main() {
    int i,n,d[2n];
    scanf("%d\n",&n);
    for(i=0;i<2n;i++)
    scanf("%d",d[i]);
    d[i]==d[i+k];
    distance==d[i+k]-d[i];
    minimum=distance[0];
    if(distance[i]<distance[0]) {
       minimum=distance[i];
        printf("%d",minimum); break; }
       return 0; }