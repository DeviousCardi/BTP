#include <stdio.h>
int main(){
int n;
int n[2*n]
scanf("%d",&n);
for(int i=0;i<2*n;i++) {
    scanf("%d",&n[i]); }
int d[n];
for(j=0;j<n;j++) {
    for(int k=j+1;k<n;k++) {
        if(n[j]==n[k])
        d[j]=j-k; } } }
int max=j[0];
for(int h=0;h<n;h++) {
    if(d[h]>max) {
        max=d[h]; } }
    return 0; }