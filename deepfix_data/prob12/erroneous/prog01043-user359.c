#include <stdio.h>
int main(){
    int n,a[200],i=0,j,k=1,d;
    scanf("%d ",n);
    scanf("%d",&a[0]);
    while(i<2*n){
        scanf("%d",&a[i]);
        i++; }
    for(j=0;j<2*n;j++){
        while(a[j]!=a[k]){
            k++ }
        d=k; } }
    return 0; }