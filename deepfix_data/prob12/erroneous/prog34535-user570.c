#include <stdio.h>
int main(){
    int i,j,ent[n],ext[2n];
    for(i=1;i<=n;i++) {
        for(j=n+1;j<=2n;j++)
        ent[i]=ext[j];
        scanf("%d%d",&i,&j);
        if((j-i)==MIN(i,j)
        printf("%d",MIN(i,j));
    return 0; }