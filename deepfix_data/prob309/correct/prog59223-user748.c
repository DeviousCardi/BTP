#include <stdio.h>
int main() {
    int n;
  int a[n];
  int count=0;
  for(int i=1;i<=n;i++)
  scanf("%d",&a[i]); {
      if(n>0)
      count =count+1;
     int i;
     if(a[i]==i) {
           printf("YES"); }
       else {
           printf("NO"); } }
    return 0; }