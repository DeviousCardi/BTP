#include <stdio.h>
int main()
{ int n,i,j;
  scanf("%d %d %d",&n,&i,&j);
  int a1[n];
  int count;
  for(int i=0;i<n;i++) {
  scanf("%d",&a1[i]); }
  while(a1[i]==j) {
      count=count+1; } {
      if(count==j) {
          printf("Yes"); }
      else {
          printf("No"); } }
    return 0; }