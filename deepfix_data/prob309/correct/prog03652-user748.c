#include <stdio.h>
int main() {
    int n;
  int array[n];
  int count=0;
  for(int i=1;i<=n;i++)
  scanf("%d",&array[i]); {
      if(n>0)
      count =count+1;
     int i;
     if(array[i]==i) {
           printf("YES"); }
       else {
           printf("NO"); } }
    return 0; }