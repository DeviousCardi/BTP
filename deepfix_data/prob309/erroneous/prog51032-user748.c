#include <stdio.h>
int main() {
    int n;
  int array[n];
  for(int i=1;i<=n;i++)
  scanf("%d",&array[i]); {
      for(int j=1;j<=n;j++)
        int i=0;
       if(array[i]==j) {
           printf("YES"); }
       else {
           printf("NO"); } }
    return 0; }