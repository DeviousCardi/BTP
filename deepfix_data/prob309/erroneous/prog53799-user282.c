#include <stdio.h>
int main()
{ int n;
  scanf("%d %d %d",&n,&i,&j);
  int a1[n];
  int i,j,count;
  for(int i=0;i<n;i++) {
  scanf("%d",&a1[i]); }
  while(a1[i]==j) {
      count=count+1; } {
      if(count==j) {
          printf("Yes"); }
      else {
          printf("No"); } }
    return 0; }