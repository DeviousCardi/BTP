#include <stdio.h>
int main() {
    int n,count=0;
    int s[];
    sacnf("%d",n);
    for(i=0;i!=EOF;i++) {
        scanf("%d",&s[i]);
        for(j=0;j!=EOF;j++) {
            if (s[i]==s[j]);
            count=count+1;
            else {
                break; } } }
    if(count==s[i]) {
        printf("Yes"); }
    else {
        printf("No"); }