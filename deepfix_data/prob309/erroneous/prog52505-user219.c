#include <stdio.h>
int main() {
  int i,j,n,s,num,count;
  int[n];
  scanf("%d",&[n]);
  int S[20];
  for(i=0;i<n;i++) {
     scanf("%d",&num);
     S[i]=num;
     count=0;
  for(j=0;j<n;j++)
  {if (s[j]==num)
  printf("Yes");
  else {
      printf("No"); }
    return 0;