#include <stdio.h>
#include <stdlib.h>
int main() {
	for(i=1;i<=h;i++) {
	    for(j=1;j<=n;j++) {
	        if(j<=c)
	        printf(" ");
	        else
	        printf("*"); }
        if(i<=((h-1)/2)); {
	      c++;
	      n--; }
	    else {
	      c--;
	      n++; } }
	return 0; }