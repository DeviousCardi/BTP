#include <stdio.h>
#include <stdlib.h>
int main() {int=i,j,k,n;
scanf("%d",&n);
for {
    (i=1;1<=n;i++) {
        for(j=1;j<=i;j++)
        printf("%d",i);
        for(k=1;k<=n-i+1;k++)
        printf("%d",k); }
	return 0; }