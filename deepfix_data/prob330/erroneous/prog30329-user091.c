#include <stdio.h>
int main() {
    int n,m=0,p,i;
    scanf("%d",&n);
    int peak[n];
    for(i=0;i<n;i++) {
        scanf("%d",&peak[i]);
        printf("%d",peak[i]); {
           ( peak[i-1]<peak[i]&&peak[i]>peak[i+1] }
    return 0; }