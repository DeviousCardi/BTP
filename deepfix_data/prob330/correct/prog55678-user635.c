#include <stdio.h>
int main() {
    int i,q,r;
    int n[i];
    int sum=0;
   r = (q>=3 && q<=100);
    scanf("%d",&r);
    for(i=1;i>=1 && i<=(q-1);i++) {
       if(n[i]>n[i-1] && n[i]>n[i+1]) {
           sum = sum +1; }
       else {
               continue; } }
   printf("%d",sum);
    return 0; }