#include <stdio.h>
int main(){
    int n,i,k,a;
    scanf("%d",&n);
    int arr[n];
    a!=0;
for(i=a;3<i<=n-1;i++) {
    scanf("&d",&arr[n]);
if((arr[i]>arr[i+1]) && (arr[i]>arr[i+2])) {
printf("arr[i] is max");
k=i++; } }
    return 0; }