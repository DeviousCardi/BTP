#include <stdio.h>
int main(){
    int n,i;
    scanf("%d",&n);
    int arr[n];
    scanf("%d",&arr[n]);
for(i=0;3<i<=n;i++) {
if((arr[i]>arr[i+1]) && (arr[i]>arr[i+2])) {
printf("arr[i] is max"); } }
    return 0; }