#include <stdio.h>
int main(){
    int n,i;
    scanf("%d",&n);
    int arr[n];
    scanf("%d",&arr[n]);
for(i=0;3<i<=n;i++) {
if((arr[i]>arr[i+1]) && (arr[i]>arr[i-1])) {
printf("arr[i]",i++); } }
    return 0; }