#include <stdio.h>
#include <stdlib.h>
	int fac(int n) {
	fac=1;
	if(n==0) {
	    return 0; }
	else
	for(i=0;i<n;i++) {
	    fac=fac*i; }
	return fac; } {
    int catalan(int n)
int main() {
	return 0; }