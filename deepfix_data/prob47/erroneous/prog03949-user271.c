#include <stdio.h>
#include <stdlib.h>
int fact(int n){    int i,sum=1;
    if(n==0)
    return 1;
    else{
    for(i=0;i<=n;i++) {
        sum*=sum*i;
    }}return sum; }
int catalan(int n ){
    if (n==0)
    return 1;
    t= }
int main() {
	return 0; }