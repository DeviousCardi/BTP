#include <stdio.h>
int main() {
    int i,k,n=0;
    int arr[n];
    int len( int arr[i]);
    scanf("%d",&n);
    scanf("%d",arr[i]);
    while(i<=n,arr[i]!='\0') {
        n++; }
    if(arr[i]%2!=0) {
        printf("lucky");
    }else{
        printf("unlucky"); }
    return 0; }