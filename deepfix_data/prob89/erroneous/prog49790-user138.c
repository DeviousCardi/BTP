#include <stdio.h>
int main(){
int k,n;
   scanf("%d",&k);
   scanf("%")
   int n[100];
   for(int i=0;i<100;i++) {
          scanf("%d")
          a[i]=i+1; }
      for(int j=i;j<100;j++)
         {if((a[j]+a[j+1]==k))
            {printf("lucky"); }
            else
               {printf("unlucky"); }