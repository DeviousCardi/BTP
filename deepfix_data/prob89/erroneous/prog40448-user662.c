#include <stdio.h>
int main() {
    int k,n=0,i;
    int arr[n];
    int len( arr[i]);
    scanf("%d",&n);
    scanf("%d",arr[i]);
    while(i<=n,arr[i]!='\0',i++) {
        n++; }
    for
    return 0; }