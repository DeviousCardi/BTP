#include <stdio.h>
int main() {
	int d,N,sum=0;
	scanf("%d%d",&d,N);
	int b[d];
	for(i=0;i<d;i++) {
	    scanf("%d",b[i]); }
	for(j=0;j<N;j++) {
	    for(i=0;i<N;i++) {
	        (a[j]==b[i]); } }
	for(j=0;j<N;j++) {
	    sum=sum+a[j]; }
	printf("%d",sum);
	return 0; }