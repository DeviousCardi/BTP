#include <stdio.h>
int main() {
	int d,n,var;
	scanf("%d %d\n",&d,&n);
	int arr[50];
	for(int a=0;a<d;a++) {
	    scanf("%d ",&var);
	    arr[a]=var; }
	if(n<d)
	printf("%d",arr[n]);
	else {
	    for(int c=) }
	return 0; }