#include <stdio.h>
int rec(int k,int d,int l[k],int m[d]) {
    int i,j;
    for(j=0;j<k;j++) {
    if(j<d)
        a[k]=b[k];
    else {
    for(i=j-d;i<j;i++) {
        a[t]=a[t]+a[i]; } } }
    return a[k]; }
int main() {
    int n,d;
    scanf("%d %d",&d,&n);
    int b[d];
    for(i=0;i<d;i++)
        scanf("%d",&b[i])
    int a[n];
    a[n]=rec(n,d,a[n],b[d]);
    printf("%d",a[n]);
    return 0; }