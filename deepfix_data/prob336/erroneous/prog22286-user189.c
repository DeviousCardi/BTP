#include <stdio.h>
#include <stdlib.h>
int main() {
	int a,b,c;
	scanf("%d%d%d",&a,&b,&c);
	if((a+b)>c)); }
	else {
	 printf("Cannot form a triangle"); }
	if((b+c>a)) }
	else{
	    printf("Cannot form a triangle"); }
	if((a+c>b)) }
	else {
	    printf("Cannot form a triangle");
	 if((a*a+b*b)>(c*c)){
	 printf("Acute Triangle"); }
	 else {
	     printf("Not Acute Triangle"); }
	 if((b*b+c*c)>(c*c)){
	 printf("Acute Triangle"); }
	 else{
	     printf("Not Acute Triangle"); }
	 if((a*a+c*c)>(b*b)){
	     printf("Acute Triangle"); }
	 else{
	     printf("Not Acute Triangle"); }
	 return 0; } } } } } } } } } } }
	return 0; }