#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
	int a,b,c;
	scanf ("%d%d%xd",&a,&b,&c);
	if  (a+b>=c);
	   if ((a*a+b*b)>(c*c)); {
	     prinf("Acute Triangle";) }
	  else {
	      printf ("Not Acute Triangle";) }
    else ((a+b)<c))  {
        printf("Cannot form a triangle"); }
	return 0; }