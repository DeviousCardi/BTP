#include <stdio.h>
#include <stdlib.h>
int main() {
	int a,b,c;
	scanf("%d",&a);
	scanf("%d",&b);
	scanf("%d",&c);
	 if (cond1 a+b<c|| cond2 b+c<a || cond3 a+c<b )
	 printf(" cannot form a triangle\in");
	 else
	 (a*a+b*b<c*c);
	 printf("actute triangle\in");
	 else
	 ((a*a+b*b)=(c*c));
	 printf(" not actute triangle\in");
	return 0; }