#include <stdio.h>
#include <stdlib.h>
int main() {
    int a,b,c;
    scanf ("%d ,%d ,%d",&a ,&b, &c);
    if {a+b>c ||b+c>a ||a+c>b ;
    then {if a*a+b*b>c*c ||b*b+c*c>a*a ||a*a+c*c>b*b;
    then printf ("triangle is accute");
    else printf("triangle is obtuse"); }
    else printf ("triangle is not possible"); }
	return 0; }