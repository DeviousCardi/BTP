#include <stdio.h>
#include <stdlib.h>
int main() {
    int x=3;
    scanf("%d",x);
    int y=5;
    scanf("%d",y);
    int z=6;
    scanf("%d",z);
    if((x*x)+(y*y)>(z*z))
    printf("Acute Triangle");
    if((x*x)+(y*y)<(z*z))
    printf;("Not Acute Triangle");
    else
    printf(Cannot from a Triangle);
	return 0; }