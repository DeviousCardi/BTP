#include<stdio.h>
     int main(){
        int N;
        scanf("%d",&N);
        int arr[N];
        int i,m;
        for(i=0;i<N;i++)
            scanf("%d",&arr[i]);
            void swap(int a,int b);
            int t,a,b;
            t=a;a=b;b=t;
            if(N/2!=0)
            m=N/2;
            printf("%d",m);
            if(N/2==0)
            m=N/2;
           printf("%.d",m);
           return 0; }