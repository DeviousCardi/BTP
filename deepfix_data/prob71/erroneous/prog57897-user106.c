#include <stdio.h>
int main() {
    int n,max;
    scanf("%d",&N);
    int a[100000]
    for(i=0;i<n;i++) {
        scanf("%d",&a[i]); {
            if(a[i]>max) {
            max=a[i]; }
            printf("%d",a[i]); } }
    return 0; }