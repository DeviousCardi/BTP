#include <stdio.h>
#include <stdlib.h>
#include <math.h>
  int main() {
	int n,i;
	    scanf("%d\n",&i);
    int a[i];
	    printf("%d\n",a[i]);
	for(i=0;i<n;i++) {
	int  m;
        m=pairs(a[i]);
	    m=set(m);
} int a,b,l;
  a=a[m1];
  b=a[m2];
  a!=b;
  l=(a,b);
	    printf("%d\n",l);
	return 0; }