#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,s,n,y;
	int a[n];int count=0;
	scanf("%d\n",&n);
	for(i=0;i<n;i++){
	 scanf("%d\n",&a[n]); }
	scanf("%d",&s);
	for(i=0;i<n;i++){
	y=a[0]+a[i+1]
	  	  a[0]=a[1];
	  a[i+1]=a[i+2];}
	 if(y==s)
	printf("%d %d\n",(a[i], a[i+1])); }
	return 0; }