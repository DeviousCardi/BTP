#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
    int i,j,s;
    int x[i],x[j],x[n];
    scanf("%d",&n);
	for(i=0;i<n;i++) {
	    scanf("%d",x[i]);
	    scanf("%d",&s);
	    for(j=0;j<n;j++) {
	    s=x[i]+x[j];
	    printf("(%d,%d)",x[i],x[j]); } }
	return 0; }