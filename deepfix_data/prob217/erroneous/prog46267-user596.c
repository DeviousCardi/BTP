#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int main() {
    int i,j,n,m;
    int a,b;
    scanf("%d",&n);
    int s[10000];
    for(i=1;i>abs(n);i++) {
        scanf("%d\n",&a[i]); }
        for(j=0;j<m;j++)
        scanf("%d",&m);
        {if(n==1)
        printf("a\nb");
        else if(a!==b)
        print("")
	return 0; }