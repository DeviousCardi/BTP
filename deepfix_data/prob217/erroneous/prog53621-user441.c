#include <stdio.h>
#include <stdlib.h>
int main() {
   int n,i,s,j,k,sum;
   int a[n];
   scanf("%d\n",&n);
   for(i=0;i<n;i++) {
       scanf ("%d\n",&a[i]);}
   scanf ("%d\n",&s);
   for(j=0;j<n;j++) {
    for (k=0;k<n;k++) {
     sum = a[k]+a[j] }
     if sum = s
       printf ("(%d,%d)",a[i],a[j]); } }
	return 0; }