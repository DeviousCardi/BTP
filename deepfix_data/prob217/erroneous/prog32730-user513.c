#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
	int N=10000,S,E,a,b,i,n,p;
	    scanf("%d%d%d",&p,&S,&E);
	  for (i=0;i<=N;i++) {
	           if(s=a+b)
	           printf("%d(a,b)");
	           else continue; }
	return 0; }