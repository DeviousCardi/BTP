#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,s,i;
	scanf("%d",&n);
	int input[n];
	for(i=0;i<n;i++) {
	    scanf("%d",&input[i]); }
	for(i=0;i<n;i++) {
	    printf("%d",a[i]); }
	return 0; }