#include <stdio.h>
#include <stdlib.h>
int main() {
    int n,i,j,k,l;
  int  count=0;
    scanf("%d",&n);
    scanf("%d",&k);
	int A[n];
        char ch;
        for(i=0; i<n; i++){
            ch=getchar(); }
        for(j=0; j<n; j++){
            ch=getchar(); }
        for(l=k; l<k-n; l++){
            ch=getchar(); }
        if(A[j]+A[l]==k){
       putchar("(A[i],A[l])",); }
          count++;
	return 0; }