#include <stdio.h>
#include <stdlib.h>
int main() {
    int N,array[10000],S;
    int i,j;
    scanf("%d",&N);
    for(i=0;i<=N-1,i++)
    scanf("%d",a[i]);
    scanf("%d",&S);
    for(i=0;i<N;i++) {
    for(j=0;j<N;j++) {
        if(a[i]+a[j]==S)
        printf("(a[i],a[j])") }
	return 0; }