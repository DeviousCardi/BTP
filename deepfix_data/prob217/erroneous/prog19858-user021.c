#include <stdio.h>
#include <stdlib.h>
int main() {
	int N,s,i,m,j;
	int arr[10000];
	scanf("%d\n",&N);
	for(i=0;i<n;i++){
	    scanf("%d",&arr[i]); }
	scanf("\n%d",&s);
	for(i=0;i<n;i++){
	    m=s-arr[i];
	    for(j=0;j<n;j++){
	        if(m==arr[j]){
	            printf("(arr[i],arr[j)");
	            break; }
	        else{
	            continue; } } }
	return 0; }