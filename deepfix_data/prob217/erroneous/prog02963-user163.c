#include <stdio.h>
#include <stdlib.h>
int main() {
    int n,i,b;
    scanf("%d\n",&n);
    int a[n];
    for(i=0;i<=n;i++) {
        scanf("%d\n",a[i]); }
    for(i=0;i<b;i++) {
        scanf("%d\n",a[b]); }
    for(i=0;i<=n;i++) {
        a[i]>=(pow(-10,5))&&a[i]<=(pow(10,5)); }
    scanf("%d\n",&sum);
    int sum;
    sum>=pow(-10,5)&&sum<=pow(10,5);
	return 0; }