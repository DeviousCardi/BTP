#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
	int i,n;
	scanf("%d\n",&n);
	for(i=0;i<n;i++) {
	    int s,a[i],count=0; {
	        scanf("%d\n",&a[i]); }
	   count++;
	  if (a[i]+count==s); }
        print("%d",(a[i],count));
	return 0; }