#include <stdio.h>
#include <stdlib.h>
int main() {
	int a,b,i,v,s[n];
	a=0;
	for(i=0;i<=n;i++)
	{scanf("%d",&s[i]); }
	printf("%d",&v);
	for(a=0;a<=v/2;a++){
	    if(a==s[i]){
	        b=v-a;
	    }printf("%d%d",&a,&b); }
	return 0; }