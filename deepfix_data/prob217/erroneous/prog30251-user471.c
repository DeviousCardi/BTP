#include <stdio.h>
#include <stdlib.h>
int main() {
    int N,S;
    int arr[pow(10,4)];
    scanf("%d%d",N,S);
for(i=0;i<=N-1;i++)
for(j=i+1;j<=N-1;j++)
if(arr[i]==arr[j])
   printf("%d"(i,j))
	return 0; }