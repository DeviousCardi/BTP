#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
	       int a[n],i,j;
	       long int S,n;
	       scanf("%ld",&S);
	       scanf("%ld",&n);
	       for(i=0;i<=n;i++) {
	           for(j=0;j<=n;j++) {
	               if((i+j)==S)
	               printf("(i,j)");
	               continue; } }
	return 0; }