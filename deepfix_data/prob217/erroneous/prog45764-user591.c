#include <stdio.h>
#include <stdlib.h>
int main() {
int a,b,c,d;
int n; ;
scanf("%d",&a[i]);
scanf("%d",&n);
for(a[i]=0;a[i]<=n;a[i]=a[i]+1){
    printf("%d",a[i]); }
    if((a)+(b)=(c)+(d)){
printf("(a,b)"); }
	return 0; }