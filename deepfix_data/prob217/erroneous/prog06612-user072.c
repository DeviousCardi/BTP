#include <stdio.h>
#include <stdlib.h>
int main() {
	int N, i, S, a,b;
	scanf("%d", &N, &S);
	int array[N];
	{if(i=0 ; i<=N ; i++){
	    scanf("%d", &array[N]); }
	{if(i=0; i<=N; i++){
	    if(a+b=S){
	        printf("(a,b)");}
	   else{
	   printf(""); }
	printf("\n");}
	return 0; }