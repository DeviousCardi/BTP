#include <stdio.h>
#include <stdlib.h>
int main() {
	       int n,i,j;
	       int a[n];
	       scanf("%d",&n); {
	           for(i=0;i<n;i++)
	           scanf("%d",&a[i]); }
	       int s;
	       scanf("%d",&s);
	       for(i=0;i<n;i++)  {
	           for(j=i+1;i<n;j++)
	       if(s==a[i]+a[j]);
	    printf("%d%d\n",a[i],a[j]); }
	return 0; }