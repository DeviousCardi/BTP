#include <stdio.h>
#include <stdlib.h>
int main() {
	int j,i,n;
	scanf("%d",&n);
	for(i=0;i<n;i++) {
	    int count,a[i],S; {
	        scanf("%d",&a[i]); }
	    if(a[i]+a[i+1]==S)
	    count++;
	    printf("(a[i],a[j])"); }
	return 0; }