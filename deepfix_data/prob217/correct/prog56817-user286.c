#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,i;
	scanf("%d",&n);
	int arr[n];
	scanf("%d",&i);
	while(i<=n){
	    scanf("%d",&arr[i]); }
printf("%d",arr[i]);
	int s;
	scanf("%d",&s);
	return 0; }