#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
    int n,i,b,c,sum;
    scanf("%d\n",&n);
    int a[20];
    for(i=0;i<=n;i++) {
        scanf("%d",a[i]); }
    for(i=0;i<b;i++) {
        scanf("%d",a[b]);
        a[b]<=pow(10,5)&&a[b]>=pow(-10,5); }
    for(i=0;i<c;i++) {
        scanf("%d",a[c]);
        a[c]!=a[b];
        a[c]<=pow(10,5)&&a[b]>=pow(-10,5); }
    sum>=pow(-10,5)&&sum<=pow(10,5);
    scanf("%d",&sum);
    if (sum==a[b]+a[b]) {
        printf("%d,%d",a[b],a[b]); }
	return 0; }