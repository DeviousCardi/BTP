#include <stdio.h>
#include <stdlib.h>
int main() {
	int a,b,i,v,n,s[n];
	for(i=0;i<n;i++)
	{scanf("%d",&s[i]); }
	scanf("%d",&v);
	for(a=0;a<=v/2;a++){
	    if(a==s[i]){
	        b=v-a;
	    }printf("(%d,%d)\n",&a,&b); }
	return 0; }