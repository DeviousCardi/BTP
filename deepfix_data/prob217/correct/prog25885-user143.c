#include <stdio.h>
#include <stdlib.h>
int main() {
	int j,i,n,s=0;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	for(j=0;j<n;j++) {
	    int count,a[i]; {
	        scanf("%d",&a[i]); }
	    if(a[i]+a[i+1]==s)
	    count++;
	    printf("(a[i],a[j])"); }
	return 0; }