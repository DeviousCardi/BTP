#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
    int n,i,b,sum;
    scanf("%d\n",&n);
    int a[20];
    for(i=0;i<=n;i++) {
        scanf("%d\n",a[i]); }
    for(i=0;i<b;i++) {
        scanf("%d\n",a[b]); }
    for(i=0;i<=n;i++) {
        a[i]>=(pow(-10,5))&&a[i]<=(pow(10,5)); }
    scanf("%d\n",&sum);
    sum>=pow(-10,5)&&sum<=pow(10,5);
    if (sum==a[b]+a[b]) {
        printf("%d\n",(a[b],a[b])); }
	return 0; }