#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,i,a[10],k,j,s;
	int b[10];
	scanf("%d\n",&n);
	for(i=0;i<n;i++) {
	    scanf("%d",&a[n]); }
	scanf("%d\n",&s);
	for(k=0;k<n;k++) {
	for(j=n;j>0;j--) {
	    if (s==(a[b[j]]+a[b[k]])){
	 printf("(%d,%d)",a[b[k]],a[b[j]]); }
	  continue; } }
	return 0; }