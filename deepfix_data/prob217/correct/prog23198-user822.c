#include <stdio.h>
#include <stdlib.h>
int main() {
    int n;
	scanf("%d",&n);
	int i;int sum;
	int a[n];
	for(i=0;i<n;i++) {
	    scanf("%d",&a[i]); }
for(i=0;i<n;i++) {
	    printf("%d",a[i]); }
	scanf("%d",&sum);
	int j;
	return 0; }