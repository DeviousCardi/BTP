#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,n,S;
	scanf("%d",&n);
	for(i=0;i<n;i++) {
	    int count,a[i]; {
	        scanf("%d",&a[i]); }
	    if(a[i]+a[i+1]==S)
	    count++;
	    printf("(a[i],a[i+1])"); }
	return 0; }