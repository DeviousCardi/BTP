#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,n,s=0;
	scanf("%d\n",&n);
	for(i=0;i<n;i++) {
	    int c,b,t,a[i]; {
	        scanf("%d\n",&a[i]); }
	   c=a[i];
	   b=a[i+1]; {
	       if(c+b==s)
	     t=(c,b); }
	    printf("%d",t); }
	return 0; }