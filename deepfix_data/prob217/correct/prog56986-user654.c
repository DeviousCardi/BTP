#include <stdio.h>
#include <stdlib.h>
int main() {
	       int n,i,x,y,s;
	      int a[10000];
	       scanf("%d",&n);
	       for(i=0;i<n;i++)
	       scanf("%d",&a[i]); {
	           printf("%d\n",(x,y)); }
	       x+y==s;
	return 0; }