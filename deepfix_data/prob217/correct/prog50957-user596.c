#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int main() {
    int i,j,n,m,s;
    int a,b;
    scanf("%d",&n);
    int p[10000];
    for(i=2;i<(10000);i++) {
        scanf("%d\n",&p[i]); }
        for(j=1;j<i;j++)
        scanf("%d",&m);
        s=a+b; {
        if(a<b)
        printf("a\nb");
        else
        printf("b\na"); }
        if(a==b)
        printf(" ");
        printf("\n");
	return 0; }