#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,n;
	scanf("%d\n",&n);
	for(i=0;i<n;i++) {
	    int s=0,c,b,t,a[i],count=0; {
	        scanf("%d\n",&a[i]); }
	   c=a[i];
	   b=a[i+1];
	   count++; {
	       if(c+b==s)
	     t=(c,b); }
	    printf("%d",t); }
	return 0; }