#include <stdio.h>
int main(){
        int i,j,x,n=0;
    for(i=1;i<=n;i++)
        for(j=1;j<=n;j++)
            {scanf("%d",&x);}
                if((i==j)&&(i==1)||(i!=j)&&(i==0))
      printf("GIVEN n*n matrix is an IDENTITY MATRIX");
 else
     printf("GIVEN n*n matrix is not an IDENTITY MATRIX");
        return 0; }