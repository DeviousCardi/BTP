#include <stdio.h>
int main(){
 int i,j,n=0,a;
 for(i=1;i<=n;i=i+1) {
     for(j=1;j<=n;j++)
{  scanf("%d",&a);} }
  if(((i==j) &&(i==1))||((i==!j)&&(i==0)))
  printf("GIVEN nxn matrix is an IDENTITY MATRIX");
  if (((i==!j)&&(i==!0))||((i==j)&&(i==!1)))
 printf("GIVEN nxn matrix is NOT an IDENTITY MATRIX");
    return 0; }