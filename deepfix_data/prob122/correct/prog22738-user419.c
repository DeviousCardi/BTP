#include <stdio.h>
int main(){
      int x,y,z;
      scanf("%d%d%d",&x,&y,&z);
      if(x==1&&y==z==0){
          if(y==1&&x==z==0){
              if(z==1&&x==y==0){
                  printf("GIVEN 3 x 3 matrix is an IDENTITY MATRIX"); } } }
      else{
          printf("GIVEN 3 x 3 matrix is NOT an IDENTITY MATRIX"); }
    return 0; }