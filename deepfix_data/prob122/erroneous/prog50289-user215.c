#include <stdio.h>
int main(){
    int n,i,j,a;
    scanf("%d",&n);
    for(i=1;i<=n;i++) {
        for(j=1;j<=n;j++)
        scanf("%d",&a);
        if(i==j)a==1;
        else a==0; }
        printf("GIVEN %dx%d matrix is an IDENTITY MATRIX",n,n);
        break; }
    scanf("\n");
    if(n!=a)
    break; }
    if(i==n+1) {
    printf("GIVEN %dx%d matrix is an IDENTITY MATRIX",n,n); }
    return 0; }