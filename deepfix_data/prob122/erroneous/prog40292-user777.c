#include <stdio.h>
int main(){
    int n;
    int i,j;
    scanf("%d%d",&i,&j);
    for(i=1;i<=n;i++)
        for(j=1;j<=n;j++){
            scanf("5d",&x);
                if(i==j)
                {if(x!=1)
        printf("GIVEN n*n matrix is an IDENTITY MATRIX"); } }
    else {
        printf("GIVEN n*n matrix is not an IDENTITY MATRIX"); }
        return 0; }