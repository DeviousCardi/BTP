#include <stdio.h>
int main(){
    int n;
    int i, j;
    int row, col;
    scanf("%d\n", &n);
    for(j=1; j<=n; j++){
        for(i=1; i<=n; i++){
            scanf("%d ", &row);
        \n } }
    if (i==j){
        if (row==0)
        printf("yes"); }
    return 0; }