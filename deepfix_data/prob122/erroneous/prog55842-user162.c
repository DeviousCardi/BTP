#include <stdio.h>
int main(){
 int i,j,n,a;
 scanf("%d",&n);
 for(i=1;i<=n;i++) {
     for(j=1;j<=n;j++) {
      scanf("%d",&a); } }
         if(i==j==1&&i!=j==0)
        { printf("Given nxn matrics is an IDENTITY MATRIX\n"); }
        else
        { printf("Given nxn matrics is NOT an IDENTITY MATRIX\n"); } }
    return 0; }