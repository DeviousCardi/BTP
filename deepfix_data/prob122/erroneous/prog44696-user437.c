#include <stdio.h>
int main(){
 int n;
 int i;
 int j;
 scanf("%d",&n);
 scanf("%d%d",&i,&j);
 for(i=1; i<=n; i++){
     for(j=1; j<=n; j++);}
     if((i=j)&&(j=i)&&(i=1)) {
     printf("GIVEN n*n matrix is an IDENTITY MATRIX"); }
   else( if
       for(i!=j)
       (if(i!=0)&&(j!=0))
       printf("GIVEN n*n matrix is an IDENTITY MATRIX"); }
    return 0; }