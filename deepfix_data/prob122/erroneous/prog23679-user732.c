#include <stdio.h>
int main(){
    int n,i,j,e;
    scanf("%d",&n);
   {  for(i=1;i<=n;i=i+1)
  {for(j=1;j<=n;j=j+1)
  scanf("%d",&e);
   if(i==j)
   {if(1==e)
        {if(i!=j)
            {if(0==e) }
        }        {printf("GIVEN 3X3 matrix is an IDENTITY MATRIX");}
    flag==1;
    return 0; }