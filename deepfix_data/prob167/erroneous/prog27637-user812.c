#include<stdio.h>
int sum(arr[],size)
    { sum(arr,100)
    { if sum(arr,0)
      return 0;
     else
     return sum(arr,N-1)+arr[N-1]; } }
int main() {
    int N ,arr[100];
    scanf("%d",&N);
    for(int i=0;i<N;i++) {
        scanf("%d",&arr[i]); } }
    return 0; }