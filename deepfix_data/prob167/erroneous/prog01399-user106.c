#include<stdio.h>
int n,i;
 int sum( int arr[n],)
for(i=0;i<n;i++) {
    sum=sum+a[i] }
if(n==0)
return 0;
else
return sum;
int main() {
    int sum=0;
    int n;
    scanf("%d",&n);
     int arr[n];
    for(i=0;i<n;i++) {
        scanf("%d",&arr[i]); }
    printf("%d",sum);
    return 0; }