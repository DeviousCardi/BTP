#include<stdio.h>
int add(a[n] n);
int main() {
    int n,sum=0;
    scanf ("%d",&n);
    sum=sum+a[n];
    printf("%d",sum);
    return 0; }
    int add(a[n],n) {
        if(n!=0);
        sum=a[n]+a[n-1]; }