#include <stdio.h>
int main(){
    int n,a[n],i,num,lowest;
    scanf("%d",&num);
    for (i=0; i<n; i++){
        scanf("%d",&a[i]); }
    a[0]=lowest;
    if(a[i]<lowest){
        lowest=a[i]; }
printf("%d",lowest);
    printf("end");
    return 0; }