#include <stdio.h>
int main(){
   int n,i,max,min,temp;
   scanf("%d",&n);
   int a[n];
   for(i = 0;i<n;i++) {
       scanf("%d",&a[i]);
   if (a[max]>a[i]) {
       max = i; } {
       if (a[min]<a[i])
       min = i;
    }}
    temp = a[i];
    max = min;
    min = temp;
   printf("%s",a[i]);
    return 0; }