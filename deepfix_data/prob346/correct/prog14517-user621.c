#include <stdio.h>
int main(){
    int n,ch,cost;
    scanf("%d",&n);
    int random[n];
    int ordered[n];
    scanf("%d",&ch);
    for(int i=0;i<n;i++) {
        random[i]=ch;
        scanf("%d",&ch); }
    int i2=0;
    for(cost=0;cost<=99;cost++) {
        for(int i=0;i<n;i++) { } }
    for(int i=0;i<n;i++) {
        printf("%d ",ordered[i]); }
    printf("end");
    return 0; }