#include <stdio.h>
int main(){
    int i,n,a;
    int c[n];
    scanf("%d",&n);
        for (i=1;i<=n;i++)
       scanf("%d",&c[n]);
    for(i=1;i<=c[n];i++)
    if(c[n]>c[n-1])
    {printf("end");}
    return 0; }