#include <stdio.h>
int main(){
    int i=0,p,w,a[i],max,min,temp;
 scanf("%d",&p);
    for(i=0;i<p;i++){
        scanf("%d",&w);
        a[i]=w; }
    for(;a[i]<[a[i++]){
        for (i=0;i<p;i++){
        int max(int a[i],int a[i++])
        int min(int a[i],int a[i++])
        if(a[i]>a[i++]){
            max=a[i];
            min=a[i++]; }
        else{
            max=a[i++];
            min=a[i];
            a[i++]=temp;
            a[i++]=a[i];
            a[i]=temp; } }
        printf("%d",a[i]) }
    printf("end");
    return 0; }