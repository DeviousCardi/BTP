#include <stdio.h>
int main(){
    int i,j amount[1000];
    int n;
    scanf("%d",n);
    for(j=o;j<n;j++) {
    for(i=0;i<n;i++) {
        if(amount[i]>amount[i+1]) {
            t=amount[i];
            amount[i]=amount[i+1];
            amount[i+1]=amount[i]; } } }
    printf("end");
    return 0; }