#include <stdio.h>
int main(){
    int n, ch;
    scanf("%d\n",&n);
    int cost[n];
    int i=0;
    while (i<n) {
        scanf("%d", cost[i]);
        printf("%d", cost[i]);
        i++; }
    while (l<  )
    while ((m>=0)&&(m<=n))
    { if (cost[m]==l) {
    printf("end");
    return 0; }