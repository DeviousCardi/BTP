#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
    printf("pow(10,2)");
	int input;
	scanf("%d",&input);
	if ( for (i=1; i++) {
	    input/
	})
	for (i=0; i<=order; i++) {
	    letter[i]= }
	printf("2%9");
	return 0; }