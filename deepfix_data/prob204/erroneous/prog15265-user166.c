#include <stdio.h>
#include <stdlib.h>
int main() {
	int n=10;
    for(i=0;i<n;i++) {
        char s[i];
        scanf("%c",&s[i]);
        count=count+1; }
    for(i=0;i<n;i++) {
        for(j=count-i;j>=0;j++) {
          char num[j]=s[i]; } }
    if(num[n]==s[n]) {
        printf("Yes"); }
    else {
        printf("No"); }
	return 0; }