#include <stdio.h>
#include <stdlib.h>
int power(int x){
    power =1;
    for(i=0;i<x;i++){
        power = power*10 }
    return power; }
int power(int x);
#include <math.h>
int main() {
	int n,a[17];
	scanf("%d",&n);
	for(i=16;i>=0;i--){
	    if(n%power(i)=n) {
	        a[i]=0; }
	    else{
	        a[i]=n%power(i) } }
	for(i=16;i>=0;i++){
	    if a [i] }
	return 0; }