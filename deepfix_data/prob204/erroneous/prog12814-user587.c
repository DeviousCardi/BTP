#include <stdio.h>
#include <stdlib.h>
int main() {
    int a[n],i,n;
    char ch;
    ch=getchar();
    for(i=1;i<=n;i++)
    scanf("%d",&a[n]);
    for(i=1;i<=n;i++) {
        ch=a[n];
        if(a[i]==a[n-i])
        printf("YES");
        else
        printf("NO"); }
	return 0; }