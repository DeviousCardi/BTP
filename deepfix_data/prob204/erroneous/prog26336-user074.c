#include <stdio.h>
#include <stdlib.h>
int main() {
int i,j;
char A[500];
scanf("%s",A[]);
for(j=0j<=500;j++) {
    if(A[j]==0)break;
    return j; }
for(i=0;i<=j-1;i++)
while(A[i]!=A[j-i-1])continue; {
    if(A[i]!=A[j-i-1])
    printf("No");
    else
    printf("Yes"); }
	return 0; }