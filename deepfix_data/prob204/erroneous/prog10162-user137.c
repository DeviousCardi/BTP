#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
	int i,m,n,j,count=0;
	scanf ("%d",&n);
	for (i=1;i<=n;i++){
	for (i=1;i<=5;i++){
	    j=pow(10,i);
	    k=pow(10,)
	   m=(n%j)* }
	return 0; }