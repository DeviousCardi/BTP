#include <stdio.h>
#include <stdlib.h>
int reverse(int n)
int s=0;
int i=o;
for(i=0;i<=n;i++)
s=n%10/pow(10,i)*pow(10,i);
int main() {
	int n;
	scanf("%d",&n);
	p=reverse n;
	if(y==1)
	printf("Yes");
	else
	if(y==0)
	print("No");
	return 0; }