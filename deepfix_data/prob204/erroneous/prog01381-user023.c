#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,p,n;
    scanf("%d",&p);
    for(i=1;i<=p;i++){
        a[n]=a[i];
        scanf("%d",a[n]);
        if(a[n]=a[n-i]){
            printf("YES"); }
        else{
            printf("NO"); } }
	return 0; }