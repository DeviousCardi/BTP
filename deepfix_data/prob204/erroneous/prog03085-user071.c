#include <stdio.h>
#include <stdlib.h>
    read_into_arrayint[n]
       int num[n];
       int i;
       for(i=0;i<n;i++){
           scanf("%d",&num[i]); }
int main()
	  int n,j,x; {
	  if(j=1;j<n;j++){
	      scanf("%d",&x);
	      read_into_arrayint[n];
	      printf("%d",num[i]); }
	return 0; }