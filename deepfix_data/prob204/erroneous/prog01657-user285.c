#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int main() {
	int num,a;
	   int sum=0;
	scanf("%d",&num);
	while(a!=0){
	    a=num%10;
	    num=num/10;
	    sum=10*sum+a;
	  b=sum; }
     	printf("%d",b);
	return 0; }