#include <stdio.h>
#include <stdlib.h>
int main() {
    int n,j,ai,a(n+1-i);
    scanf("%d",&n);
    for(j=0;j<n;j++) {
        if(ai==a(n+1-i)) {
            printf("YES"); }
        else
           printf("NO"); }
	return 0; }