#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,j,n;
    int a[i];
    scanf("%d",&a[i]);
    for(i=1;i<=n;i++)
    scanf("%d",&a[j]);
    for(j=1;j<=i;j-=j) {
        if(a[i]=a[j])
        printf("Yes");
        else
        printf("No"); }
    	return 0; }