#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,i,a[10],j;
	scanf("%d",&n);
    for(i=1;i<=(j/2);i++)
    {   scanf("%d",&a[j]);
        if(a[i]==a[j-i+1]) {
            printf("Yes"); }
        else
            printf("No"); }
	return 0; }