#include <stdio.h>
#include <stdlib.h>
int main() {
	int n;int i;int a[i];
	for(i=1;i<=n;i++)
	{scanf("%d",&a[i]);}
	if (a[i]>0)
	   { if( a[i]==a[n-i])
	printf("Yes");
	else
    printf("No");}
		return 0; }