#include <stdio.h>
#include <stdlib.h>
int main() {
    int a[20],i,n;
    for(i=1;i<=n;i++)
    scanf("%d",&a[n]);
    for(i=1;i<=n;i++) {
        if(a[i]==a[n-i])
        printf("YES");
        else
        printf("NO"); }
	return 0; }