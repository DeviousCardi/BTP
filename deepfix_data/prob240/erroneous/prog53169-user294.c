#include <stdio.h>
#include <stdlib.h>
  int catalan(int n)
  int ans=0;
  int catalan(int n)
  {    if(catn=0)
       return 1;
       else
    {  if(catalan=ans)
       ans=(4*n+2)*catalan/(n+2)
       return ans; } }
int main() {
	int n,testcases,k,i,j;
	scanf("%d",&tastcases);
	for(i=0; i<tastcases; i++) {
	 scanf("%d",&k);
	 k==catalan;
	 printf("%d\n",k-1); }
	return 0; }