#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,n,j,a[20];
	int a[n];
	scanf("%d\n",&n);
	for(i=0;i<n;i++) {
	    scanf("%d\n",&a[n]); }
	for(j=0;j<n;j++) {
	if(j==0) {
	    if(a[j]<a[j+1])
	      printf("Yes");
	    else
	      printf("No"); }
	else {
	   if(a[n]<=[n+1] && a[n]<=a[n-1]) {
	      printf("Yes\n"); }
	   else {
	      printf("No\n"); } } }
	return 0; }