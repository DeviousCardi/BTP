#include <stdio.h>
#include <stdlib.h>
int main() {
     int n;
     scanf("%d",&n);
     int a[n];
        for(i=1;i<(n-1);i++){
            if(a[i]<a[i-1]&&(a[i]<a(i+1))
            {k++; }
        else if(a[i]>a[i-1]&&a[i]>a[i+1])
        {k++; } }
    if(k>0)
    {printf("yes"); }
	return 0; }