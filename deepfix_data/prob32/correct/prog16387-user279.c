#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,n,u;
	int array[n];
	scanf("%d\n",&n);
	for(i=0;i<n;i++) {
	    scanf("%d\n",&array[n]); } {
	   if(array[n]<=array[n+1] && array[n]<=array[n-1]) {
	      u=1; }
	   else {
	      u=0; }
	 }if(u==1) {
	     printf("Yes"); }
	 else {
	     printf("No"); }
	return 0; }