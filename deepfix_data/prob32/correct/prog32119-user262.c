#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,j,t,n;
	int a [20];
	for(i=0;i<n;i++) {
	    scanf("%d",&a[i]);
	    if (a[i]%2==0) {
	        j=j+1; } }
    if (j%2==0) {
        printf("Yes"); }
    else {
        printf("No"); }
	return 0; }