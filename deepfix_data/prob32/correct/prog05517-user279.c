#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,n,a[20];
	int array[n];
	scanf("%d\n",&n);
	for(i=0;i<n;i++) {
	    scanf("%d\n",&array[n]); }
	if(a[n]<=a[n+1] && a[n]<a[n-1]) {
	   printf("Yes\n"); }
	else {
	    printf("No\n"); }
	return 0; }