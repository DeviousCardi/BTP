#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,i,j,sum;
	int arr[n];
	scanf ("d",&n);
	for(j=0;j<n;j++) {
	    if ((arr[n]>arr[n-1])&&(arr[n]>arr[n+1]))
	    printf ("yes");
	    else
	    printf ("no"); }
	return 0; }