#include <stdio.h>
#include <stdlib.h>
int main() {
	        int  i,n,gap;
	        scanf("%d",&n);
	        for(gap=2;gap>=1;gap=gap-1) {
	            printf(") }
	        for(j=1;j<=n+1;j=j+2){
	        for(i=1;i<=j;i=i+1)
	        if(i%2==0) {
	            printf("x"); }
	        else {
	            printf("*"); } }
	return 0; }