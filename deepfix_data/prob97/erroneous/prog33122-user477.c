#include <stdio.h>
#include <stdlib.h>
int main() {
	int a,b,c,d,e,f;
	scanf("%d",&a);
    for(b=0;b<a;b++) {
        for(c=1;c<2*a-1;c++)
        if((j>=a-b)&&(j<=a+b))
        printf("%d",a-c);
        else
        printf(" "); }
	return 0; }