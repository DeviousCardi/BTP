#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,j ,s;
            int n>0;
            for(i=1;i<=n;i++)
            {for(s=0;s<(n-i);s++)
                {printf(" "); }
                {for(J =1;J<=(2*I-1); J++)
                {printf("%d",s++%10);} } } }
	return 0; }