#include<stdio.h>
int main() {
    int a,b,c;
    int max;
    scanf("%d%d%d",&a,&b,&c);
    if(a>b){
        if(a>c) max=a;
        else max=; }
    else{
        if(b>c) max=b;
        else max=c; }
    if(a+b>c&&b+c>a&&a+c>b){
        if( }
    else printf("INVALID");
    return 0; }