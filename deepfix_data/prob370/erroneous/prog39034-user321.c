#include<stdio.h>
int main()
{   int a,b,c;
scanf ("%d%d%d",&a,&b,&c);
if {triangle is invalid
    ((a+b)>c);
    {printf ("invalid",triangle); }
    else { printf ("valid",triangle); } }
if triangle is acute  {
    ((a*a + b*b)>=(c*c));
    {printf ("acute",triangle); }
else {printf ("not acute",triangle); }
if triangle is right {
    ((a*a + b*b)= (c*c));
    {printf ("right",triangle); }
    else {printf ("not right",triangle); }
    if triangle is obtuse {
    ((a*a +b*b)<= (c*c));
    { printf ("obtuse",triangle); } } } }
    return 0; }