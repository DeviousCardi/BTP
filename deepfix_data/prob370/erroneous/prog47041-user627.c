#include<stdio.h>
int main() {
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
    if((a+b<c)||(b+c<a)||(c+a<b)) { }
    else if((a*a+b*b=c*c)||(b*b+c*c=a*a)||(c*c+a*a=b*b)) { }
    else if((a*a+b*bc*c){}
    return 0; }