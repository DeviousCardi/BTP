#include<stdio.h>
int main() {
    int a,b,c,x,y,z;
    scanf("%d%d%d",&x,&y,&z);
    if(c>b&&c>a)
    if((a*a+b*b)>(c*c))
    {printf("ACUTE");}
    else if((a*a+b*b)==(c*c))
    {printf("RIGHT");}
    else if((a*a+b*b)<(c*c))
    {printf("INVALID");}
    return 0; }