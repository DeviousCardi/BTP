#include <stdio.h>
int main(){
 int bd, i ,rem,sum=0;
 scanf("%d",&bd);
 while(bd!=o) {
     rem=bd%10;
     sum=sum+rem;
     bd=bd/10; }
 for(i=2016; ;i++) {
     if((i%sum)==0) {
        printf("%d",i);
        break; } }
    return 0; }