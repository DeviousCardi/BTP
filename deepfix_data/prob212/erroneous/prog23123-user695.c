#include <stdio.h>
int main() {
    int birthyear[4],luckyyear[];
    scanf("%d",&birthyear[4]);
    if(luckyyear[]>={2,0,1,6}&&(luckyyear[])/(birthyear[0]+birthyear[1]+birthyear[2]+birthyear[3])==0) {
        printf("%d",luckyyear[]); }
    return 0; }