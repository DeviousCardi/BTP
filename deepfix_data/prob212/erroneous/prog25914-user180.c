#include <stdio.h>
int main(){
    int birthyear,sum=0,x;
    scanf("%d",&birthyear);
    while(birthyear!=0){
        x=birthyear%10;
        sum+=x;
        birthyear=birthyear/10; }
    while(y!=1){
        int i=0,y=0;
        if((2016+i)%s==0)
            y=1;
        i++; }
    i--;
    printf("%d",2016+i);
    return 0; }