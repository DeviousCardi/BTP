#include <stdio.h>
int main() {
    int x,y;
    int n;
    int a,b,c,d;
    scanf("%d %d",&x,&n);
    n=(1000*a+100*b+10*c+d);
    y=a+b+c+d;
    if(a<=9 && b<=9 && c<=9 && d<=9)
    printf("%d",77*y+x%y);
    return 0; }