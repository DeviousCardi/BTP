#include <stdio.h>
int main(){
    int n,rem,i;
    int x=0,y=0;
    int sum=0;
    scanf("%d",&n);
    while(n!=0) {
            rem=n%10;
            n=n/10;
            sum=sum+rem; }
    for(i=0;i>2016;i++) {
           y=i/n;
            y=0; }
        printf("%d",x);
      return 0; }