#include <stdio.h>
int main(){
    int m,n,count;
    scanf("%d",&n); {
        for(m=2;m<=n-1;m++) {
            if(m%n==0)
            count++;
            scanf("%d",&count); {
                if(count==0)
                printf("YES");
                else
                printf("NO"); } } }
    return 0; }