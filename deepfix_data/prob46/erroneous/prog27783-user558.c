#include <stdio.h>
int main() {
    int n,i,t;
    t=n;
    for(i=1;i<=n;i++) {
        t=t%i;
        if(t==0)
        continue; }
    if(m==t-2)
    printf("yes");
    else
    printf("no");
    return 0; }