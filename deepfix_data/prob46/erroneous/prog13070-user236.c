#include <stdio.h>
int main(){
    int m,n;
    int i;
    int count=0;
    scanf("%d",&a);
    scanf("%d",&b);
    scanf("%d",&m);
    for (i=2,i<=n/2,i++) {
            if(n%i==0) {
                count=count+1; }
            else }
    return 0; }