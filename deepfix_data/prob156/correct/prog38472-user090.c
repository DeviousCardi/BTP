#include <stdio.h>
int main() {
    int m,n,c,i,j ;
    int sum[n] ;
    scanf("%d%d",&n,&m);
    for(i=1;i<=n;i++)
    {for(j=1;j<=m;j++)
     {sum[i]=sum[i]+1 ;
     scanf("%d",&c); }
     printf("%d",sum[n]); }
    return 0; }