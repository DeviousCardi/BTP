#include <stdio.h>
int main() {
    int n,m,i,sum,x,j,y=0;
    scanf("%d %d",&n,&m);
    for(i=1;i<=n;i++) {
        for(j=1;j<m;j++) {
            scanf("%d",&x);
            sum=sum+x; }
        if(sum>y); }
    printf("%d",y);
    return 0; }