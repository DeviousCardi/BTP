#include <stdio.h>
    int max(int a,int b);
    if(a>b)
    return a;
    else
    return b; {
   int main(){
       int n,m;
   int count=0;
   scanf("%d %d",&n,&m);
   for(int i=1;i<=n;i++) {
       for(int j=1;j<=m;j++)
     count=count++; {
      if(a>b) {
          printf("a"); }
      else {
      printf("b"); } } }
    return 0; }