#include <stdio.h>
int main() {
    int n,m,i,a;
    int s[n];
    scanf("%d%d",&n,&m);
    for (i=0;i<n;i++) {
      sum=0;
        for (j=1;j<=m;j++) {
          scanf("%d",&a);
          sum=sum+a; }
         s[n]=sum; }
    return 0; }