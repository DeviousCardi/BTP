#include <stdio.h>
int main() {
    int room, heap, gold, sum, max;
    scanf("%d %d\n", &room, &heap);
    max=0;
    for(int i=0,i<room;i++){
        for(int j=0,j<heap,J++){
            scanf("%d", gold);
            sum=0;
            sum=sum+gold; }
        if(max<sum)
            max=sum; }
    printf("%d",sum);
    return 0; }