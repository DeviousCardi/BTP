#include <stdio.h>
int max(int a,int b) {
    if(a>b)
    return a;
    else
    return b; }
int main() {
    int i,j,n,m,a;
    scanf("%d %d\n",&n,&m);
    for(i=1;i<=n;i++) {
        for(j=1;j<=m;j++) {
            scanf("%d",&a);
            ci=ci+a; }
        m=max(ci,c) }
    return 0; }