#include <stdio.h>
int main() {
    int n,m,max,s;
    scanf("%d %d",&n,&m);
    for(j=0;j<m;j++)
               { scanf("%d",&a);
                s=s+a;
                max=s;}
    for(i=1;i<n;i++) { {
                for(j=0;j<m;j++)
                scanf("%d",&a);
                s=s+a;
            };
            if(max>=s)
            max=s; }
    printf("%d",max)
    return 0; }