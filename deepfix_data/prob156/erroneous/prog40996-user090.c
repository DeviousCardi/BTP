#include <stdio.h>
int main() {
    int m,n,c,i,j ;
    scanf("%d%d",&n,&m);
    for(i=1;i<=n;i++)
    {for(j=1;j<=m;j++)
    int sum[i]=0 ;
     {scanf("%d",&c);
     sum[i]=sum[i]+c;}
    }printf("%d",sum);
    return 0; }