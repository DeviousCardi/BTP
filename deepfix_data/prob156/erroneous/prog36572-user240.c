#include <stdio.h>
int main() {
    int n,m,i,j,a,k=0,count=0;
    scanf("%d %d",&n,&m);
    for(i=0;i<n:i++) {
        k=0
        for(j=0;<m;j++) {
            scanf("%d",&a);
            k=k+a;
        };
        if(k>count) {
            count=k;
        };
    };
    printf("%d",count);
    return 0; }