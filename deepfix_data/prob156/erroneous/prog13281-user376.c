#include <stdio.h>
int main() {
    int n,m
    scanf("%d %d",&n,&m);
    for(i=1;i<=m;i++) {
        scanf("%d",&h[i ]);
        s1=s1+h[i]; }
    for(i=1;i<=m;i++) {
        scanf("%d",&h1[i]);
        s2=s2+h1[i]; }
    if(s1>s2)
        printf("%d",s1)
    else
        printf("%d",s2);
    return 0; }