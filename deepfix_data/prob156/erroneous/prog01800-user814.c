#include <stdio.h>
int main() {
    int n,m;
    int num;
    scanf("%d %d",&n,&m);
    int i,j;
    for(i=1;i<=n;i++) {
        for(j=1;j<=m;j++) {
            scanf("%d",&num); }
        if(i>=2) {
            if() } }
    return 0; }