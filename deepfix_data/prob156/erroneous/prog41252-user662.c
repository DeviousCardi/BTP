#include <stdio.h>
int main() {
    int n,m,i=0,j=0;
    int max=0,sum1=0,sum2=0;
    int a[i],b[j];
    scanf("%d%d",&n,&m);
    if(n<2) {
        for(i=0;i<m;i++) {
        scanf("%d",&a[i]);
        sum1=sum1+a[i]; }
        max=sum1;
    }else{
        for(j=0;j<m;j++) {
        scanf("%d",&arr[j]);
        sum2=sum2+arr[j]; }
        max=sum2; }
    return 0; }