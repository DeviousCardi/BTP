#include <stdio.h>
int main() {
    int n,i,t,k;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++) {
        scanf("%d/n",&a[i]); }
    for(i=0;i<n;i++) {
        for(j=0;j<n;j++) {
            if(arr[j]==arr[i]) {
                t=j-i;
                k=i; } } }
    printf("%d",t);
    printf("%d",k);
    return 0; }