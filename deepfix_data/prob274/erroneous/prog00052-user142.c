#include <stdio.h>
#include<math.h>
int main(){
    int n,i,x,k,t;
    scanf("%d",&n);
    int ar[n];
    for(n=1;2<=n<=100;) {
    for(x!=i;1<=x<=n;) {
        t=x;
        k=(n-t)/2; }
   printf("%d",t);
   printf("%d",k);
    return 0; }