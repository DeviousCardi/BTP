#include <stdio.h>
int main(){
int n;
scanf("%d",&n);
int room[n];
for(i=0;i<n;i++) {
    scanf("%d",&room[i]); } {
    for(k=room[0];k<n;k=room[k])
    a[k]=room[k]; }
for(k=0;k<n;k++) {
    for(i=0;i<n;i++) {
        if(a[i]==a[k])
        break; } }
printf("%d %d",i,k);
    return 0; }