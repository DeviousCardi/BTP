#include <stdio.h>
int main(){
    int n, i, k, j, a, c, flag = 0, time =0, revisit = 0;
    scanf("%d", &n);
    int arr[n];
    for(i = 0; i < n; i++){
        scanf("%d", &arr[i]); }
    for(i = 0; i < n i++){
        printf("%d", arr[i]); }
    printf("%d ", time);
    printf("%d", j);
    return 0; }