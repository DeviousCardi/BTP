#include <stdio.h>
int main() {
    int a[101],b[100],n,count=1,count2=0,i;
    scanf("%d\n",&n);
    for(i=0;i<n;i++) {
        scanf("%d",&a[i]);
    }for {
        if(a[0]!=a[i] && a[i]!=1)
            count=count+1; } }
    printf("%d %d",count,count-1);
    return 0; }