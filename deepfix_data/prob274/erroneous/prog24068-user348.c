#include <stdio.h>
int main(){
    int n,sum;
    scanf("%d",&n);
    int p[n];
    for (i==;i<=n;i++){
        scanf("%d",&p[i]);
        sum=0;
        sum=sum+i
       if(p[0]==p[i]);
       break; }
    printf("%d",sum);
    return 0; }