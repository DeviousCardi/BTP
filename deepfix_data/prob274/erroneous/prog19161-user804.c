#include <stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int a[100];
    int i,x;
    for(i=2;i<=n;i++) {
           scanf("%d",&a[i]); }
     for(x=1;x<=n;x++) {
             scanf("%d",&x);
             (if x!=i) }
    return 0; }