#include <stdio.h>
int main(){
    int n,a[100],t,k;
    int i;
    scanf("%d",&n);
    if(n<=100 && n>=2) {
        for(i=0;i<n;i++) {
            scanf("%d",a[i]); }
        for(i=0;i<n;i++)
        {if(t==k) }
    }printf("%d",t);
    printf("%d",k);
    return 0; }