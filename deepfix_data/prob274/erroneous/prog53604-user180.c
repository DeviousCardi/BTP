#include <stdio.h>
int main(){
    int n,tmp,t=0;
    scanf("%d",&n);
    int list[100];
    int i=0;
    while(i<n){
        scanf("%d ",&list[i]);
        i++; }
    int k=0;
    for(int k=0;k<n;k=tmp,t++){
         tmp=list[k];
        if(l[0]==tmp)
            break; }
    printf("%d",t);
    return 0; }