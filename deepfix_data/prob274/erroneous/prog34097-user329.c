#include <stdio.h>
int main() {
    int i,n, room[105],t,k,current;
    scanf("%d", n);
    room[0]=1;
    for(i=1; i<n; i++) {
            scanf("%d", room[i]); }
    for(i=0; i<n; i++) {
            current = room[i];
            if(current == ) }
    return 0; }