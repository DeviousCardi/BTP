#include <stdio.h>
int main(){
    int n,i;
    int a[1000]
    scanf("%d",&n);
    for (i=0 ; i<n ; i++) {
        scanf ("%d", a[i]); }
    int b=0,c;
    while (a[0] != a[k]) {
        c=a[0];
        a[c]=a[k];
        a[k]=c
        b++ }
    printf("%d",b);
    return 0; }