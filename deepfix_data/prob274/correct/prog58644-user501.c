#include <stdio.h>
int main(){
    int n,i,t;
    int in[100];
    scanf("%d",&n);
    for(i=0;i<n;i++)
        scanf("%d",&in[i]);
    if(in[i]==1)
        {t=0;
        t=t+1;
        printf("%d",t);}
    return 0; }