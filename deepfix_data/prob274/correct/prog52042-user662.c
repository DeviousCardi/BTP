#include <stdio.h>
int main(){
    int n,x=0,i=0,t=0,k=0;
    int  arr[i];
    scanf("%d",&n);
        while(2<=n<=100) {
        scanf("%d",&arr[i]);
        for(1<=x<=n;x!=i;i++) {
            printf("%d",t); } }
    printf("%d",k);
    return 0; }