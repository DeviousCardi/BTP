#include <stdio.h>
int main() {
    int i,n,x,room[n],counter;
    scanf("%d",&n);
    x=room[1];
    if((2<=n)&&(n>=100))
    for(i=1;i<=n;i++) {
        while(room[i]==0)
        counter++; }
    if(counter==1)
    printf("%d",x);
    return 0; }