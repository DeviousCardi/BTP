#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
          a = 3;
          b = 4;
          c = 5;
    if (a <= b ){
        printf("%d",b); }
    if (b <= c) {
        printf("%d",b); }
	return 0; }