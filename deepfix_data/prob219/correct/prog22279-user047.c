#include <stdio.h>
#include <stdlib.h>
int main() {
	       int a,b,c,d;
	       scanf("%d%d%d",&a,&b,&c);
	               if(a>b) {
	                   if(a>c)
	                   d=a;
	                   else
	                   d=c; } {
	                   if(c>b)
	                   d=c;
	                   else
	                   d=b; }
	               printf("%d",d);
return 0; }