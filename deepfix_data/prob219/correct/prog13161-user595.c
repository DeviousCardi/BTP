#include <stdio.h>
#include <stdlib.h>
int main() {
	int a,b,c;
	scanf("%d",&a);
	if(a<b)
	    if (a<c)
	    printf("%d",c);
	    else
	    printf("%d",a);
	else
	    if(b<c)
	    printf("%d",c);
	    else
	    printf("%d",b);
	return 0; }