#include <stdio.h>
int arr[100]={0};
int n,u;
int getMaxLessThan(int u) {
    int u;
    do {
        int i=0;
        if(arr[i]>=u) {
            u=arr[i]; }
        i++; }
    while(i<n); }
int main() {
    int u=-999;
    int i=0;
    do {
        scanf("%d",&arr[i]);
        if(arr[i]>=u) {
            u=a[i]; }
        i++; }
    while(i<n);
    upperLimit=
    return 0; }