#include <stdio.h>
int arr[100]={0};
int n;
int getMaxLessThan(int upperLimit) {
    int j;
    for(j=0;j<n;j++){
        if(arr[j]<=upperLimit)
            upperLimit =a[j]; } }
int main() {
 int k, i;
  scanf("%d %d",&n,&k);
 for(i=0; i<n; i++)
   scanf("%d", &arr[i]);
    return 0; }