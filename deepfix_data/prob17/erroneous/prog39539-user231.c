#include <stdio.h>
int main() {
	int a,b,c=0,d=0,e,f,g;
	scanf(%d,&a);
	int y[20],z[20];
	do{
	    y[c]=getint();
	    c++; }
	while(c<a);
	scanf(%d,&b);
	do{
	    z[d]=getint();
	    d++; }
	while(d<b);
	putint(z);
	return 0; }