#include <stdio.h>
int main() {
    int count,i,j,arr1[20],arr2[20],n1,n2;
    scanf("%d",n1);
    scanf("%d",arr[n1]);
    scanf("%d",n2);
    scanf("%d",arr[n2]);
	for(i=0;i<n1;i++)
	for(j=0;j<n2;j++) {
	    while(arr[j]=arr[i])
	    count=1;
	    while(arr[j]!=arr[i])
	    count=0; }
	if(count==1)
	printf("YES");
	else
	printf("NO");
	return 0; }