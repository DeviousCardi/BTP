#include <stdio.h>
int main() {
	int a,i,j,N1,N2;
	    scanf("%d",&N1);
	int A1[N1],A2[N2];
	for(i=0;i<N1;i++) {
	    scanf("%d ",&A[i]);
	   scanf("%d",&N2);
	   int A2[N2];
	   for(j=0;j<N2;j++) {
	      scanf("%d ",&A[j]);
	      for(a=0;a<i;a++) {
	            if(A[i]==A[j]);
	                printf("YES") } } }
	return 0; }