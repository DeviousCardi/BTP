#include <stdio.h>
#include <stdlib.h>
int main() {
	int
	scanf("%d",&n);
	for (m=0;m<n;m++) {
	    scanf("%d",&a[]); }
	for (i=0;i<n-2;i++) {
	    if (a[i]<a[i+1]) {
	        if(a[i+1]>a[i+2]) {
	            printf("Yes");
	            d=1; } } }
	if (d==0)
	printf("No");
	return 0; }