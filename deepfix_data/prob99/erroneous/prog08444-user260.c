#include<stdio.h>
int max(int a,int b) {
    if(a>b)
    return a;
    else return b; }
int Max(int 1+x[],int n)
{    int x[n];
    return max(Max(1+x[],n-1),1+x[n]); }
int main() {
   int i,j,a[20];
   scanf("%d",&n);
   for(i=0;i<n;i++)
   scanf("%d",a[i]);
    return 0; }