#include<stdio.h>
#include<strings.h>
int getReverse(int[])
int main() {
   int n,rev,i;
    scanf("%d",&n);
    int ar[100];
    scanf("%d",&ar);
  for(i=0;i<=n;i++) {
    rev=getReverse(ar[100]);
    printf("%d",a); }
    return 0; }
  intgetReverse(ar[100]){
      if(i<n){
          rev=getReverse(ar[100]); }
      return rev; }