#include<stdio.h>
#include<strings.h>
int reverce( int  ) {
    int i,j;
    for(i=0; i<n; i++) {
        scanf("%d",&b[i]); }
    for(i=0; i<n; io++) {
        if(b[i]/2=) } }
int main() {
    int n;
    int b[100];
    int k,a
    scanf("%d",&n);
    k=reverce(a);
    return 0; }