#include<stdio.h>
#include<strings.h>
int main() {
    int i,n,re;
    scanf("%d\n",&n);
    int a[n];
    for(i=0;i<n;i++)
    scanf("%d ",&a[i]);
    re=rev(a[i]);
    printf("%d ",re); }
int rev(int a[]) {
    int m,j;
    for(j=m;j<0;j++)
    return (a[j]); }