#include<stdio.h>
int product(int num[]) {
    int a;
    int pro=product(n-1)*n; }
int main() {
    int N;
    scanf("%d",&N);
    int num[N];
    int i,product;
    for(i=0; i<N; i++) {
        scanf("%d",&num[i]);
        scanf(" "); }
    int product=product(num);
    return 0; }