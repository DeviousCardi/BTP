#include<stdio.h>
int prod( int a[],int size) {
    int N=0,i=0;
    if(N==0) {
        return N; }
    else {
        for(i=1;i<=N;i++) {
           int ans=1;
           int prod=a[i]*ans;
            return prod; } } }
int main() {
    int N,i;
    int a[1000];
    int prod(a[i]);
    printf("%d",x);
    return 0; }