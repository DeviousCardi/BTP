#include<stdio.h>
int main()
{  int a,c,i=0,prodt;
    if( a>=2 && a<=1000000000) {
     scanf("%d",&a); }
  int arr[a];
    for(i=0;i<a;i++) {
    scanf("%d",&arr[i]);
    c = arr[a-1]*prodt((n-1)*arr[n-1]; }
    printf("%d",c);
    return 0; }