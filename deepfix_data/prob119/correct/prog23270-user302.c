#include <stdio.h>
#include <stdlib.h>
int num(int x,int a,int b,int c){
    int n;
    if(n==0&&n==1)
    return 0;
    else
   return num(1,a,b,c);
   return num(x-1,a,c,b);
   return num(x-1,b,a,c); }
    int main() {
	int t,p,i,a,b,c;
	scanf("%d",&t);
for(i=0;i<t;i++){
    scanf("%d",&p); }
	return 0; }