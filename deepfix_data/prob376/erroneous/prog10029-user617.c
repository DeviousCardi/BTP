#include <stdio.h>
#include <stdlib.h>
int fac(int n) {
    if (n==0) return 1;
    else return n*fac(n-1); }
int main() {
	int n,t;
	int cat;
	sacnf("%d",&t);
	for(i=1;i<=t;i++)
	scanf("%d",&);
	cat = fac(2n)/(fac(n+1)*fac(n));
	printf("%d",cat)
	return 0; }