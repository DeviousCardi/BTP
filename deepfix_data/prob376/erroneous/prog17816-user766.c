#include <stdio.h>
#include <stdlib.h>
int catalan(int n) {
    int i,sum=0;
    if(n==0)
    return 1;
    else
    return
    for(i=0;i<=n-1;i++) {
    sum=sum+catalan(i)*catalan(n-1-i); } }
int main() {
	return 0; }