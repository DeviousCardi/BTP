#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,a[10000],i,value,x=5000;
	scanf("%d",&n);
	for(i=1;i<=n;i++) {
	scanf("%d",&value);
	a[i]=value; }
	if(a[i]==a[x])
	printf("%d",3);
	return 0; }