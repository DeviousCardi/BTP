#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,a[10000],i,value=0,x=0;
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	a[i]=value;
	scanf("%d",value);
	if(a[i]==a[x])
	printf("%d",a[x]);
	return 0; }