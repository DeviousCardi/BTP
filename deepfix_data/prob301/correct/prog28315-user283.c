#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,i,arr[50];
	scanf("%d",&n);
	for(i=0;i<n;i++) {
	    scanf("%d",&arr[i]); }
	if(arr[0]==1)
	{printf("%d",1); }
	if(arr[1]==arr[i])
	{printf("%d",arr[1]); }
	return 0; }