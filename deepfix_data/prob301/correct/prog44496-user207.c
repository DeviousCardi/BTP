#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,j,n;
    int a[n];
    scanf("%d",&n);
    int x,t,count=0;
    for(i=1;i<=n;i++){
    scanf("%d",&a[n]);
    for(j=i+1;j<=n;j++){
    if(a[i]==a[j])
    count=count+1;
    if(a[i]=x)
    break;
    }}
    printf("%d",x);
	return 0; }