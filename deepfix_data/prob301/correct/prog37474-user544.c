#include <stdio.h>
#include <stdlib.h>
int main() {
	int N,x,i,a[20],p[20],j;
	scanf("%d",&N);
	scanf("%d",&x);
	for(i=0;i<N;i++){
	  	scanf("%d",&a[i]); }
	   scanf("%d",&j);
	   for(j=0;i<N;j++){
	     scanf("%d",&p[i]);
	  if((a[p[j]]=x)&&(a[p[j+1]]=x))
	printf("%d",x); }
	return 0; }