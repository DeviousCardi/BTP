#include <stdio.h>
#include <stdlib.h>
int main() {
    int N;
	int s[N];
	int i,n,k,t;
	scanf("%d",&n);
	scanf("%d",&s[k]);
	for(i=0; i<n; i++) {
	   (s[i] < s[i+1]); }
	 scanf("%d",&t);
	 printf("%d",t);
	return 0; }