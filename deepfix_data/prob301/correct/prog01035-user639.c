#include <stdio.h>
#include <stdlib.h>
int main() {
	int a[10000];
	int i,j,n,p,k;
	scanf("%d\n",&n);
	for(i=0;i<n;i++)
	scanf("%d\n",&a[i]);
	for(j=0;j<n;j++) {
	    if(a[p]==a[k]&&p!=k)
	    i=1; }
	if(i==1)
	printf("%d",a[0]);
	return 0; }