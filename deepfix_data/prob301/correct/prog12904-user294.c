#include <stdio.h>
#include <stdlib.h>
int main() {
    int N;
	int s[N];
	int i,n,k,t;
	scanf("%d",&n);
	scanf("%d",&s[k]);
	scanf("%d",&t);
	for(i=0; i<n; i++) {
	   (s[i] < s[i+1]);
	   t=s[i]; }
	 printf("%d",t);
	return 0; }