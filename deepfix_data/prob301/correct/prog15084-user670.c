#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,n,j;
	scanf("%d",&n);
	int a[n];
	for(i=0;i<n;i++)
	{int k;
	    scanf("%d",&a[i]);
	    a[i]<=a[i+1];
	    a[i]=k;
	    a[i+1]=k;
	    a[i+2]=k;
	    printf("%d",&a[i]); }
	return 0; }