#include <stdio.h>
#include <stdlib.h>
#inclue<math.h>
int main() {
	int a[i];int j;int n;int mode;int pow;
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	scanf("%d",&a[i]);
	printf("%d\n",a[i]);
	for
	(j=1;j<=i;j++) {
	    mode=max(a[i]);
	    printf("%d",mode); }
	return 0; }