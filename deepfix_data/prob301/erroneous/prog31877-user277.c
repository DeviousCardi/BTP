#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
    int i,j;
    int n<=10000;
	int ar[n],ar[i];
	scanf("%d",ar[n]);
	    for(i=0;i<n;i++){
	        int_mode(a[i]==a[i-1] || a[i]==a[i+1])
	        printf("%d",ar[i]); }
	return 0; }