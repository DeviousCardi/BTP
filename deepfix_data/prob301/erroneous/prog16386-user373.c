#include <stdio.h>
#include <stdlib.h>
int main() {
    int a[x],i,n,j,x;
    x=10000
    scanf("%d\n %d",&n,&a[x]);
    for(i = n; i <=n; i--) {
        for(j=0; j<i; j++) {
            if(a[j]==a[i])
            {printf("%d",a[i]);} } }
	return 0; }