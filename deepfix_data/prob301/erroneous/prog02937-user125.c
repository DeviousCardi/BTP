#include <stdio.h>
#include <stdlib.h>
int main() {
    int n,i,a[10000];
    scanf("%d",&n);
    for(i=0;i<n;i++) {
        scanf("%d",a[i]) }
    scanf("%d",x);
    if(a[x]==a[x+1]==a[x+2]) {
        printf("%d",a[x]); }
    else if(a[x]==a[x+1]) {
        printf("%d",a[x]); }
    else
    break;
	return 0; }