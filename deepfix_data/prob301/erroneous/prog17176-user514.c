#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,j,count;
	int a;
	scanf("%d",a);
	int x[a];
	for(i=0;i<a;i++) {
	    scanf("%d",&x[i]);
	    for(j=0;j<i;j++) { ; }
	 return 0; }
	    ,count) }
	return 0; }