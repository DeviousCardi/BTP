#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,a[10000],i,value;
	int b,c;
	scanf("%d",&n); {
	scanf("%d",&value);
	for(i=1;i<=n;i++)
	a[i]=value; }
	{for("b=0;b<=i;b++")
	{for("c=0;c<=i;c++")
	if(a[b]=a[c])
	printf("%d",a[1]);
	}}
	return 0; }