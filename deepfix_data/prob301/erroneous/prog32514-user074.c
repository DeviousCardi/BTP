#include <stdio.h>
#include <stdlib.h>
int main() {
int a,b,c,N;
int k[10000];
      scanf("%d",&N);
      for(i=0;i<=n;i++) {
      scanf("%s",a[i]); }
      for(i=0;i<=n;i++) {
      for(j=i+1;j<=n;j++)
      if(a[j]-a[i]==0) {
         k=a[i]; }
      return k; }
  {printf("%d",k)}
	  return 0; }