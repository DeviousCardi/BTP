#include <stdio.h>
#include <stdlib.h>
int main() {
	int N;
	scanf("%d",&N);
	int a[n];
	for(i=0;i<N;i++) {
	    scanf("%d",&a[i]) }
	for(i=0;i<N;i++) {
	    if(a[i]=a[i+1]) {
	        printf("%d",a[i]);
	        break; }
	return 0; }