#include <stdio.h>
#include <stdlib.h>
int main() {
	int a[10000],i,n,j.count=o;
    	scanf("%d",&n);
    	for(i=0;i<n;i++){
        	scanf("%d",&a[i]);
	        for(j=i+1;j<n;j++){
	        if(a[i]==a[j])
	        count++;
        	printf("%d",a[i]);
        	else
	return 0; }