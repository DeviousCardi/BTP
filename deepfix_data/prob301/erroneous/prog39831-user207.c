#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,j;
    int a[n];
    int x,count=0;
    for(i=1;i<=n;i++){
    scanf("%d",&a[n]);
    if(a[i]==a[x])
    count=count+1;
    printf("%d",count);
	return 0; }