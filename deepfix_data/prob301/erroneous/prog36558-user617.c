#include <stdio.h>
#include <stdlib.h>
int main() {
	int N,i,k,n,j;
    int numb[i],;
    scanf ("%d",&N);
    for (i=0;i<N;i++)
    scanf ("%d",&numb[i]);
    for (i=0;i<n;i++) {
          for (k=0;k<n;k++)
    if ( numb[i]==numb[k])
    j++; }
    printf ("%d",numb[i]);
	return 0; }