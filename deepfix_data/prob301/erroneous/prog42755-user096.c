#include <stdio.h>
#include <stdlib.h>
int count (int a[20]){
 if   (a[i]==a){
     return i; } }
int max (int a,int b) {
    if (a>b)
        return a;
        else return b; }
int main() {
	long long int a[n],n;
	scanf ("%d",&n);
	for (i=0;i<n;i++) {
	scanf ("%d",&a[i]);
	count (a[i]);
	max (counta[i],a[i-1]); }
	printf ("%d",max);
	return 0; }