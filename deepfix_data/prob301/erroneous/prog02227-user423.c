#include <stdio.h>
#include <stdlib.h>
int main() {
	int a[n],N,i,m,count=0;
	scanf("%d",&N);
	for(i=0;i<n;i++){
	    scanf("%d",a[i]);
	    for(j=0;j<N;j++){
	    if(a[i]==a[j])
	    count=count+1; } }
	return 0; }