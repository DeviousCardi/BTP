#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,N,count=0;
	int a[10*10*10*10];
	int a[i];
	scanf("%d",&N);
	   for(i=0;i<N;i++){
	       scanf("%d",&a[i]);
	       for {
	           (i=i);
	       count=count+1; } }
	return 0; }