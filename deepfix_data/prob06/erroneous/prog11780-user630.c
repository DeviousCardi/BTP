#include <stdio.h>
#include <stdlib.h>
int main() {
	int m,n,x,y,*A,*B,i,j;
	scanf("%d %d %d %d",&n,&m,&x,&y);
	A=(int*)malloc(n*sizeof(int));
	B=(int*)malloc(m*sizeof(int));
	for(i=0;i<n;i++)
	   scanf("%d",)
	return 0; }