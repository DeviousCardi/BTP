#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,n,m,x,y*A,*B;
	scanf("%d%d%d%d",&n,&m,&x,&y);
	A=(int*)malloc(n*sizeof(int));
	B=(int*)malloc(m*sizeof(int));
    scanf("%s",B);
	return 0; }