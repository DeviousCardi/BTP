#include <stdio.h>
#include <stdlib.h>
struct invent {
    char *name[20];
    int number;
    float price;
};
int main() {
    struct invent prouct[3], *ptr;
    printf("input\n\n");
    for(ptr=product;ptr<product+3;ptr++)
    scanf("%s %d %f",ptr->name,&ptr->numb);
return 0; }