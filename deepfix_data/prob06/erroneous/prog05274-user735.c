#include <stdio.h>
#include <stdlib.h>
int main() {
    int *a, *b, n, x, y, i, m, k=0, j;
    scanf("%d", &n);
    scanf("%d", &m);
    scanf("%d", &x);
    scanf("%d", &y);
    a= (int*)malloc(n*sizeof(int));
    b= (int*)malloc(m*sizeof(int));
    for(i=0; i<n; i++){
        scanf("%d", &a[i]; }
    for(i=0; i<m; i++){
        scanf("%d", &b[i]; }
	return 0; }