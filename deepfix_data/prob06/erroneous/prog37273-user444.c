#include <stdio.h>
#include <stdlib.h>
int main() {
	scanf("%d",n);
	scanf("%d",m);
	scanf("%d",x);
	scanf("%d",y);
	for(i=0;i<n;i++) {
	    scanf("%d",a[i]); }
	for(i=0;i<m;i++) {
	    scanf("%d",b[i]); }
	for(i=0;i<n;i++) { }
	return 0; }