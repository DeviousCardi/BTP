#include <stdio.h>
#include <stdlib.h>
int main() {
	long int  y=0,x=0,  *v='\n', *s='\n'
	scanf("%d %d %d %d ", &n, &m, &x, &y);
	v= (long int*)malloc(n*sizeof(long int );
	s=(long int*)malloc(m*sizeof (long int);
	for (i=0;i<=n;i++)
	scanf("%ld",(s+i);
	for (i=0;i<=m;i++)
	scanf("%ld",(v+i));
	return 0; }