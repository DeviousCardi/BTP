#include <stdio.h>
#include <stdlib.h>
void scanm(int a[],int n){
    int i
    for(i=0;i<n;i++){
    scanf("%d",&a[i]); } }
int main() {
int n,m,x,y;
scanf("%d%d%d%d",&n,&m,&x,&y);
int *a,*b;
a=(int*)malloc(n*sizeof(int));
b=(int*)malloc(m*sizeof(int));
int i,j;
for(i=0;i<n;i++){
    scanf("%d",&a[i]); }
for(i=0;i<n;i++){
    scanf("%d",&[i]); }
	return 0; }