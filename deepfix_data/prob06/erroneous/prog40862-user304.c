#include <stdio.h>
#include <stdlib.h>
int main() {
    int n,m,x,y,i,j;
    int ns*=(int*)malloc(n*sizeof(int));
    int nv*=(int*)malloc(m*sizeof(int));
    for(i=0;i<n;i++) {
        scanf("%d ",ns[i]); }
    for(i=0;i<m;i++) {
        scanf("%d ",nv[i]); }
	return 0; }