#include <stdio.h>
#include <stdlib.h>
int main() {
    int n,m,i,*s,*v,k;
    long long int x,y;
    scanf("%d%d%Ld%Ld",&n,&m,&x,&y);
    s=(int*)malloc(sizeof(int)*n);
    v=(int*)malloc(sizeof(int)*m);
    for(i=0;i<n;i++)
        scanf("%d",&s[i]);
    for(i=0;i<m;i++)
        scanf("%d",&v[i]);
    if(n==0) {
            printf("0");
            return 0; }
    for()
    return 0; }