#include <stdio.h>
#include <stdlib.h>
	void scan(long int *p,int n){
	    int i;
	    for(i=0;i<n;i++){
	        scanf("%1d",&p[i]); }
	    return; }
int main() {
    int f,g;
    long int *a,*b,l,m;
    long int *soldiers_,*vest_;
    scanf("%d",&a,&b);
    scanf("%1d %1d",&l,&m);
    a=(long int*)malloc(f*sizeof(long int));
    b=(long int*)malloc(g*sizeof(long int));
    soldiers=(long int*)malloc(g*sizeof(long int));
    vest=(long int*)malloc(g*sizeof(long int));
	return 0; }