#include <stdio.h>
#include <stdlib.h>
int main() {
    int n;
    scanf("%d",&n);
    int t,a[n],m;
    scanf("%d",&t);
    int i=0;
    while(n>i) {
       a[i]=m;
    if((m>m-1)&&(m>m+1)) {
       printf("Yes");
       i++; }
    else {
        printf("No"); } }
    	return 0; }