#include <stdio.h>
#include <stdlib.h>
int main() {
	int i,j,n,t,s[t];
    char a[n] ;
	scanf("%d",&n);
	   for (i=0;i<n;i++) {
	       a[i]=getchar(); }
	   scanf("%d",&t);
	   for (j=0;j<t;j++)  {
	       scanf("%d", &s[j]); }
	return 0; }