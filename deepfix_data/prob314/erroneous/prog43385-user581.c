#include <stdio.h>
#include <stdlib.h>
int main() {
	int ch,n;
	int count=0;
	scanf("%d",&n);
	ch=get char();
	while (count<n) {
	    s[count]=ch;
	    count=count+1;
	    ch=get char(); }
	if (s[count]<s[count-1]&&s[count]<s[count]+1 (count!=0)) {
	    printf("yes"); }
	else {
	    printf("no"); }
	return 0; }