#include <stdio.h>
#include <stdlib.h>
int main() {
	int a[20],n,i,t;
	scanf("%d", &n);
	for(i=0;i<n;i++) {
	    scanf("%d", &n) }
	scanf("%d", &t);
		for(i=0;i<t;i++) {
	    scanf("%d", b[i]) }
		for(i=0;i<t;i++) {
	    if(a[b[i]]<a[b[i]-1]&&a[b[i]]<a[b[i]+1]) {
	        printf("Yes"); }
	    else printf("No"); }
	return 0; }