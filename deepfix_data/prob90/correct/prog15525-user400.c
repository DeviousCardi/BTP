#include <stdio.h>
int main(){
    int n,k,i,j,b;
    scanf("%d",&n);
     int a[n];
    scanf("%d",&k);
    for(i=0;i<n;i++) {
        a[i]=i+1; }
    for(i=0;i<n;i++) {
        k-a[i]==b;
        for(j=0;j<n;j++) {
            if(b=a[j])
            break; } }
    if(j!=n-1)
    printf("lucky");
    else
    printf("unlucky");
    return 0; }