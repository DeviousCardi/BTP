#include <stdio.h>
int main(){
    int k,i,n,a[100],sum;
    scanf("%d",&k);
    scanf("%d",&n);
    for(i=0;i<n;i++) {
        scanf("%d",&a[i]); {
            for(k=0;k<n;i++)
            {sum=0;
            sum= a[i]+a[n-k];
            if(sum==k)
            printf("lucky"); } } }
     return 0; }