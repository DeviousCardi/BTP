#include <stdio.h>
int main(){
    int k,i,n,a[100],sum;
    scanf("%d",&k);
    scanf("%d",&n);
    for(i=0;i<n;i++) {
        scanf("%d",&a[i]); {
            for(i=0;i<n;i++)
            {sum=0;
            sum= a[i]+a[n-1];
            if(sum==k);
            printf("lucky");
            break;} } }
     return 0; }