#include <stdio.h>
int main(){
    int k,n;
    scanf("%d",&k);
    scanf("%d",&n);
    int i,j;
    int num[500];
    for(i=0;i<=n;i++){
      scanf("%d",&num[i]);
       for(j=0;((j!=i)&&(j<=n);j++);
       if(num[j]==k-num[i]){
           printf("lucky")
       }else{printf("unlucky") }
    if() }
    return 0; }