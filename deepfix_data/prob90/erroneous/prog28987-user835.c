#include <stdio.h>
int main(){
    int a[500];
    scanf("%d",d);
    for(i=0;i<n,i++) {
        scanf("%d",a[i]); }
    for(h=0;h<n;h++) {
        for(k=0;k<n;k++) {
            if(a[h]+a[k]==d)
            break;
            else {
                printf("unlucky"); } } }
    return 0; }