#include <stdio.h>
int h;
int health loss(int c,int d) {
    if(c>d)
    h=h+d-c;
    return h; }
int main() {
    int p,b,c,i;
    scanf("%d",&h);
    scanf("%d",&p);
    scanf("%d",&a)
    for(i=0;i<p;i++) {
        scanf("%d",&b);
        h=health loss (int a,int b);
        if(h<=0) {
            printf("No");
            break }
        a=b; }
    if(i==p-1)
    printf("Yes");
    return 0; }