#include <stdio.h>
int main() {
    int h,n,p1,p2,flag=0,i;
    scanf("%d%d"&h,&p,&pi);
    for(i=2;i<=n;i++) {
            scanf("%d",&pf);
            if(pf<pi)
                h=h-(pi-pf)
            if(h<=0) {
                    break;
                    flag=1; } }
    if(flag==1)
        printf("No");
    else
        printf("Yes");
    return 0; }