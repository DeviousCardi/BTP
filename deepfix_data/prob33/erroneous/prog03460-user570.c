#include <stdio.h>
int main() {
    int h,p,i,q,value;                                                      scanf("%d",&h);                                                         scanf("%d",&p);
    for(i=2;i<=p;i++) {
        scanf("%d",&q);
        while(q>0)
        diff=q; {
            if(diff<=q)
            diff=q;
            if(diff>q)
            diff=diff-q; } }
    if(h>diff)
    printf("Yes");
    else
    printf("No");
    return 0; }