#include <stdio.h>
#include <stdlib.h>
int space(int a) {
    int L;
    for(L=0;L<a;L=L+1) {
        printf(" "); }
    return 0; }
int main() {
    int I,L,K;
    scanf("%d", &I);
    space(I);
    printf("test");
		return 0; }