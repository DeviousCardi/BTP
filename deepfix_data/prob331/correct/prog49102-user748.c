#include <stdio.h>
int main(){
          int i,j,n,a;
          scanf("%d %d",&i,&j);
         for(i=1;1<=n;i++) {
             for(j=1;j<=n;j++) {
            if(i==j)
           (a==1)||(a!=1);
            printf("IDENTITY MATRIX");
                if(i!=j)
                a==0;
            printf("NOT an IDENTITY MATRIX"); } }
    return 0; }