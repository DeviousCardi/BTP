#include <stdio.h>
int main(){
    int i,a,b,q[100],w[100];
    scanf("%d",&a);
    for(i=0;i<a;i++) {
    for(j=0;j<a;j++)
    scanf("%d%d",&q[i],&w[j]);
        if(i==j&&q[i]==1&&q[j]=1){
            if(i!=j&&q[i]==0&&w[j]==0){
                printf("GIven %d x %d matrix is an identity matrix",n,n); }
            else printf("GIven %d x %d matrix is not identity matrix",n,n);) } } { } }
    return 0; }