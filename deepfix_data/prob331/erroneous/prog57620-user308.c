#include<stdio.h>
int main() {
    int i,j;
    int s[100][100];
    int n,m;
    for(i=0; i<n; i++) {
        for(j=0; j<m; j++) {
        scanf("%d",&s[i][j]); } }
 int   isidentity==1;
    for(i=0; i<n; i++) {
        for(j=0; j<m; j++) {
            if(row==col && A[row][col]!=1) {
                isIdentity = 0; }
            else if(row!=col && A[row][col]!=0) {
                isIdentity = 0; } } } } }