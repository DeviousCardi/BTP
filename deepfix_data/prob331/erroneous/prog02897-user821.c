#include <stdio.h>
int main(){
    int n,i,j,a;
    scanf("%d",&n);
    if
     for(i=n;i>0;i--) {
       for(j=n;j>0;j--) {
           if(i==j) {
           a=1; }
           else {
            a=0; }
           printf("%d",a); }
    printf("\n"); }
    printf("GIVEN n x n matrix is an IDENTITY MATRIX.");
    else
     printf("GIVEN n x n matrix is NOT an IDENTITY MATRIX.");
 return 0; }