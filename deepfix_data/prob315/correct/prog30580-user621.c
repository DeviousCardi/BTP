#include <stdio.h>
#include <stdlib.h>
int main() {
    int n=0;
    scanf("%d",&n);
    int number[n];
    for(int i=0;i<n;i++) {
	   scanf("%d",&number[i]); }
	for(int i=0;1<n;i++) {
	    printf("%d",number[i]); }
	return 0; }