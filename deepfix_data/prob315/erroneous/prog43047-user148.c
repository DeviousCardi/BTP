#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,i,j=0,k;
	scanf ("%d", &n);
	int a[n];
	for (i=0;i<n;i++)
	scanf ("%d", &a[i]);
	for (i=0;i<n-1;i++) {
	    for (k=i+1;k<n;k++) {
	        if (a[i]>a[j]])
	        j++; } }
	printf ("%d",j);
	return 0; }