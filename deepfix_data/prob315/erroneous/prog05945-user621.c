#include <stdio.h>
#include <stdlib.h>
int main() {
    int n;
    scanf("%d",&n);
    int number[n];
    int inv[n];
    for(int i=0;1<n;i++) {
	    scanf("%d",&number[i]; }
	for(i2=0;i2<n;i++); {
	    for(int i3=0,i3<n;i3++) {
	        if(number[i2]>number[i3]) {
	            count++;
	            inv[i2]=count; } } }
	return 0; }