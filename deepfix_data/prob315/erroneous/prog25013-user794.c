#include <stdio.h>
#include <stdlib.h>
int main() {
    int b;
    scanf("%d",&b);
    int a[b];
    for(i=0;i<b;i++){
        for(j=i+1;j<b;j++)
        {if(a[i]==a[j]);
        count=count+1; }
int c[i]=count;
sum=sum+count;}
printf("%d",sum);
for(i=0;i<b;i++){
    printf("%d ",c[i]); }
	return 0; }