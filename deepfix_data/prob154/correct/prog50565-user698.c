#include <stdio.h>
#include <stdlib.h>
int main() {
	int n,k;
	double a,b,fxi;
	scanf("%lf",&a);
	scanf("%lf",&b);
	scanf("%d",&n);
	printf("%f",a);
	if(a<=(-1)){
	    if(b<=(-1))
	    fxi=a+k*(b-a)/n; }
	return 0; }