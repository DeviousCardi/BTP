#include <stdio.h>
#include <stdlib.h>
int main() {int n,x,i;
            double a,b,l,r;
            scanf("%lf%lf",&a,&b);
            scanf("%d",&n);
         for(i=0;i<n;i=i+1){
         if (x<=-1)
         if (x>-1&&x<1)
         if(x>=1)
         r==x*x*x; }
            printf("%.4lf",r);
	return 0; }