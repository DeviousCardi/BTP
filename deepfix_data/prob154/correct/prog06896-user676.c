#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main(){
double x,a,b,sum1;
int i,n;
double d;
d=(b-a)/n;
    for(i=0;i<n;i++) {
        scanf("%lf",&a);
        scanf("%lf",&b);
        scanf("%d",&n);
        for(x=0;x<=b;x++) {
        if(x<=-1&&a<=x&&x<=b) {
            sum1=1*d;
            sum1=sum1+1; {
                  printf("%lf",sum1); } } } }
 return 0; }