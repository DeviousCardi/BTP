#include <stdio.h>
#include <stdlib.h>
int main() {
     int n,s[n],i;
     double a,b,area,delt,sum,delsum;
     scanf("%d",&n);
     scanf("%lf%lf",&a,&b);
     for(i=0;i<n;i++){
     scanf("%d",&s[i]);
     b=s[n-1];
     a=s[0];
     delsum=0;
     for (i=0;i<n;i++){
     delt=(b-a)/n;}
     delsum=delsum+del;}
     sum=0;
     for (i=0;i<n;i++)
     sum=sum+s[i];
    for(i=0;i<n;i++){
    area= sum*deltsum; }
     printf("%.4lf",area);
    return 0; }