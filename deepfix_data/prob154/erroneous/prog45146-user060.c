#include <stdio.h>
#include <stdlib.h>
double f(double x){
    double ans;
	if (x<=-1){
	   ans=-1; }
	else if((x>-1)&&(x<=1)){
	   ans=x*x; }
	else {
	   ans=(x*x)*x; }
	ans=f;
	return ans; }
int main() {
	int n,ans=0;
	double a,b,q,i,f,x;
	scanf("%d%d%lf",&a,&b,&n);
	q=(b-a)/n;
	for(i=a;i<b;i=i+q){
	    m= }
	return 0; }