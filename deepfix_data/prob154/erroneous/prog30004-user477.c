#include <stdio.h>
#include <stdlib.h>
int main() {
    float start,end,numrect;
    input();
    float find_area(float start,float end,float numrect);
    printf("%f",&area); }
void input(void); {
    scanf("%f%f%f",&start,&end,&numrect); }
float find_area(float start,float end,float numrect) {
    float a,b,n,h
    a=start;b=end;n=numrect;
    area=float tot_area(a,b,n); }
float tot_area(float );
{float area,a=0,x;
for(i=0;i<n;i++)
if(x<=(-1))
area=a+(funcx(x)*(b-a)/2.0);
else if(x>(-1) && x<1)
area=a+(funcx(x)*(b-a)/2.0);
else if(x<=(-1))
area=a+(funcx(x)*(b-a)/2.0);
return (area);}
float funcx(float x)
{float y;
if(x=-1)
y=1;
else if(x>(-1) && x<1)
y=x*x;
else if(x<=(-1))
y=x*x*x;
return y;}
	return 0; }