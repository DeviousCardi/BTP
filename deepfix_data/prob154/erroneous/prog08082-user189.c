#include <stdio.h>
#include <stdlib.h>
#include <math.h>
double function(double n){
int main() {
  int n,i,j,n;
  double a,b,k,n;
  scanf("%f",&a);
  scanf("%f",&b);
  scanf("%d",&n);
  x[0]=a;
  k=(b-a)/n;
  for(i=1,i<n,i++){
      x[i]=x[i-1]+k; }
  sum=0 0;
  for(i=0,i<n,i++){
  sum=sum+(function(x(1)*k); }
  printf("%.4\f*",sum); }
	return 0; }