#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int main() {int n,x,i;
            double a,b,l,r;
            scanf("%lf%lf",a,b);
            scanf("%d",n);
           l=b-a\n;
         for(i=0;i<n;i=i+1){
             r=f[x]*l;
         if (x<=-1)
         r==l;
         if (x>-1&&x<1)
         r==x*x*l;
         if(x>=1)
         r==x*x*x*l; }
            printf("%.4lf",r);
	return 0; }