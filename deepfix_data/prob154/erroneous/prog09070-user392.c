#include <stdio.h>
#include <stdlib.h>
int main() {
    int n;
	double y,z,a1=0,a2=0,a3=o,ar=0,j,x;
	scanf("%d",&n);
	scanf("%lf%lf",&y,&z);
	x=(b-a)/n;
	for(j=y;j<b;j++){
	    if(j<(-1)){
	        a1=a1+x; }
	    else if(j>=(-1)&&j<1){
	        a2=a2*(j*j*x) }
	    if(i>1){
	        a2=a2*(i*i*i*x) }
	    ar=a1+a2+a3; }
	return 0; }