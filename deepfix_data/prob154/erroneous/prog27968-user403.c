#include <stdio.h>
#include <stdlib.h>
 double f(int a){
     int b,c;
     if(a<=-1)
     return -1;
     if (a>=-1 && a<=1)
     return a*a;
     if (a>=1)
     return a*a*a*; }
int main() {
	int i,n;
	double a,b;
	scanf("%lf",&a);
	scanf("%lf",&b);
		scanf("%d",&n);
		x=((b-a)/n);
		for (i=0;i<n;i++){ }
	return 0; }