#include <stdio.h>
#include <stdlib.h>
int main() {
    float dx;
	double a,b;
	int n,i,a1;
	scanf("%lf %lf %d",&a,&b,&n);
	dx=(b-a)/n;
	for(i=0;i<n,i++){
	    if((a+i*(dx))<=-1){
	        a1=1*i*(dx); } }
	return 0; }