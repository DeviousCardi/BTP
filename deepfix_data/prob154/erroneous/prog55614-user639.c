#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
	double a,b,area;
	int n;
	double f,x,delX=(b-a)/n;
	scanf("%lf",&a);
	scanf("%lf",&b);
	scanf("%d",&n);
	if(x<=-1)
	f=1;
	if(x<1&&x>-1){
	f=pow(x,2);
	for(x=-1;x<=b;x++) {
	    area=f* }
	if(x>=1)
	f=pow(x,3);
	return 0; }