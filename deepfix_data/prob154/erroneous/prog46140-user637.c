#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
	double a,b;
	int n,i;
	double x;
	scanf("%lf",&a);
	scanf("%d",&n);
	scanf("%lf",&b);
	for(x=a;i<=b;x++){
	    if(x<=(-1))
	        f(x)=1;
	    else if(x>(-1)&&x<1)
	          { f(x)=pow(x,2); }
	    else f(x)=pow(x,3); }
     for(i=0;i<=(n-1);i++){
         sum=sum+ }
	return 0; }