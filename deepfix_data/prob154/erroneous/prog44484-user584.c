#include <stdio.h>
#include <stdlib.h>
int main() {
int x,i;
int n;
int p,q;
float a,b,m;
int sum=0;
scanf("%f%f%d",&a,&b,&n);
m=(b-a)/n;
for(i=0;i<=n-1;i++) {
    if(x<=-1) {
	    p==q&&q==1;
	    sum=sum+p*m;
        printf("%d",sum) } }
	return 0; }