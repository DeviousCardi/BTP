#include <stdio.h>
#include <stdlib.h>
#include<math.h>
int main() {
	double a,b,area;
  int n;
	scanf("%lf %lf ", &a,&b);
scanf("%d",&n);
	area=(1*(-1)-a)+2+(pow(b,3))*(b-1);
    	area=b-a;
    	if(a<-1&& b>-1& b<1)
	if(a>-1&& b<1)
	if(a>1&& b>1)
	area==a*a*a(b-a);
	if(a>-1 && a<1)&& (b>1)
	area=a
	return 0; }