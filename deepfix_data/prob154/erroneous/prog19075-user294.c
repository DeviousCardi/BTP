#include <stdio.h>
#include <stdlib.h>
#include <math.h>
int main() {
	double a,b,sum;
	int n,i,area;
	scanf("%lf",&a);
	scanf("%lf",&b);
	scanf("%d",&n);
	 b > a;
	f(x)=1;  if(x<=-1)
	f(x)=x*x;  if(x>-1 || x<1)
	f(x)=x*x*x;  if(x>=1)
    for(i=a; i<=b; i++) {
        area=sum(f(xi)*dx);
     printf("%d",area); }
	return 0; }