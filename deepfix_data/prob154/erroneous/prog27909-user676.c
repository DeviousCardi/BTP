#include <stdio.h>
#include <stdlib.h>
int f(xi);
int f(xi) {
    for(i=0;i<n;i++) {
        if(x<=-1) {
            f(x)=1 ; }
        else if(-1<x<1) {
            f(x)=x*x }
        else if(x>=1) {
            f(x)=x*x*x; }
        else return -1; } }
	return 0; }