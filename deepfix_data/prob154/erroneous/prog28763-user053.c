#include <stdio.h>
#include <stdlib.h>
#include<math.h>
 double area(double) {
int i, xi,f(x),n;double a,b;double sum=0;
scanf("%d",&n);
for
(i=0;i<=n-1;i++)
area= sum(f(xi)*(b-a)/n);
printf("%lb",area);
return( double area); }
int main() {
    int x;
     if(x<=-1)
        f(x)=1;
        if(x>-1&&x<1)
            f(x)=(pow(x,2));
        if (x>=1)
            f(x)=(pow(x,3));
        double area(double);
        return 0; }