#include <stdio.h>
#include <stdlib.h>
int main() {
 int n,x;
 double a,b;
 scanf("%d",&n);
 scanf("%lf%lf",&a,&b);
 if(x<=-1) {
 return 1; }
 else {
 if(x<1&&x>-1) {
     return x*x; }
     if(x>=1) {
         return x*x*x; } }
 for(x=-1;x<=2;x++) {
     printf() }
 for(x=2;x<5;x++) {
     printf() }
 for(x=5;x<=7;x++) {
     printf() }
 for(x=7;x<=10;x++) {
     printf() }
 printf }
	return 0; }