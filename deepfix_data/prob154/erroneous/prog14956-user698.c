#include <stdio.h>
#include <stdlib.h>
double f(double a){
    if(a<=-1)
    return 1;
    if(a>=-1&&a<=1)
    return a*a;
    if(a>=1)
    return a*a*a; }
int main() {
	int n,k=0;
	double a,b,fxi;
	scanf("%lf",&a);
	scanf("%lf",&b);
	scanf("%d",&n);
	printf("%f",a);
	if(a<=(-1)){
	    if(b<=(-1))
	    fxi==a+k*(b-a)/n;
	    if(b>=-1&&b<=1) }
	return 0; }