#include <stdio.h>
#include <stdlib.h>
int main() {
    int i,j,h,k,n;
	double a,b,;
	scanf("%lf",&a);
	scanf("%lf",&b);
	scanf("%d",&n); {
	for((i=0;i<=(n-1);i++) {
  if((j<=(-1))
	for(k=0;k<=3;k++)
	pow(h,k);
	sum=sum*((pow(h,k));
	printf(".4lf"); }
	return 0; }