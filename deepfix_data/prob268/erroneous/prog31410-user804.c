#include <stdio.h>
#include <stdlib.h>
long *memarray[3];
long getways(int x, int m) {
    int y;
    y=getways(n,1) + getways(n,3) + getways(n,5); }
int main() {
    int n,i;
    scanf("%d",&n);
   long getways(n,m);
    return 0; }