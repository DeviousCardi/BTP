#include <stdio.h>
#include <stdlib.h>
long *memarray[3];
long getways(int x, int m) {
    int z = z+x%m; }
int main() {
    int n,a,z;
    scanf("%d",&n);
    a = n%5;
    for(i=0;i<a;i++){
        getways(n-5*i,3); }
    return 0; }