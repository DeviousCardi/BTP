#include <stdio.h>
#include <stdlib.h>
long *memarray[3];
long getways(int x, int m,int arr[]) { }
int main() {
    int n;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++)
    arr[n]=-1;
int d=getways(n,3,arr);
    return 0; }