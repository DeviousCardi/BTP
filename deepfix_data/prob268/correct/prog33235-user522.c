#include <stdio.h>
#include <stdlib.h>
long *memarray[3];
long getways(int x, int m) {
    if(x==5)
    return 3; }
int main() {
    long n,y=1,count=0;
    scanf("%l",&n);
    count=getways(n,y);
    printf("%l",count);
    return 0; }