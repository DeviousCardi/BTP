#include <stdio.h>
#include <stdlib.h>
long *memarray[3];
long getways(int x, int m) {
    int z = z+x%m;
   return 0; }
int main() {
    int i,n,a,z=0;
    scanf("%d",&n);
    a = n%5;
    for(i=0;i<a;i++){
        getways(n-5*i,3); }
    printf("%d",z);
    return 0; }