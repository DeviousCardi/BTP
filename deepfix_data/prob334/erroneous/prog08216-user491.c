#include <stdio.h>
#include <stdlib.h>
int main() {
	int h,b,i,j;
	scanf("%d",&h);
	scanf("%d",&b);
	for(i=0; i<h; i++){
	    int spaces = h - i - 1;
	    for(j=0; j<spaces; j++){
	        print(" "); }
	    int stars = base - (spaces*2)
	    int num = i+1;
	    for (j =0; j<stars; j++){
	        printf("%d", num%10);
	        num++ }
	    printf("\n"); }
	return 0; }