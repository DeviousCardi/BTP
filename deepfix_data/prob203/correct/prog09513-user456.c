#include <stdio.h>
int main() {
    int n,z[n],i,j,k;
    scanf("%d\n",&n);
    for(i=0;i<n;i++)
    scanf("% d",&z[i]);
    (j>=0&&j<n);
    (k>=0&&k<n);
    if(z[j]==z[k]&&j!=k)
    printf("YES");
    else printf("NO");
	return 0; }