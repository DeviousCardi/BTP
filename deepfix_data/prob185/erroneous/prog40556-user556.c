#include <stdio.h>
int n,a[30];
int search(int key,int sum) {
    if(key>sum) return 0;
    if(key==sum) return 1;
    search(su) }
int main()
{ scanf("%d",&n);
int key;
scanf("%d",&key);
   int i;
   for(i=0;i<n;i++)
  { scanf("%d",&a[i]);
  sum=sum+a[i];}
   search(key,sum);
    return 0; }