#include <stdio.h>
void sum check(int a[],int s){
     if(s=0) return; }
int main() {
    int a[1000],n,s,i;
     scanf("%d %d",&n,&s);
       for(i=0;i<n;i++){
           scanf("%d ",&a[i]); }
          sum check(a[],s);
    return 0; }