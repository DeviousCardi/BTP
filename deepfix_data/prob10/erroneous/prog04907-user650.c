#include<stdio.h>
    int max(inta,intb){
    if(a>b)
    return a;
    if(a<b)
    return b; }
int main() {
    int n,i;
    scanf("%d",&n);
    int a[n];
    for(i=0;i<n;i++)
    scanf("%d",&a[i]);
    int maxtill[n];
    return 0; }