#include <stdio.h>
#include<string.h>
int main() {
    char str[401];
    int n,i=0;
    scanf("%d",&n);
    int j=n,mini=2n;
    while(i<=2n) {
        scanf("%s",str);
        i++; }
    while(i<n) {
        while(j<2n) {
            if(str[i]==str[j])
            a=j-i;
            if(a<mini)
            mini=a; } }
    return 0; }