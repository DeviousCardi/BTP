#include <stdio.h>
int check(int pivot,int t[],int size,int total){
    int i,sum=0;
    if(pivot>=size){
        return; }
    for(i=0;i<pivot;i++){
        sum=sum+t[i]; }
    if(sum=(total-sum){
        return 1; }
    else{ } }
int main() {
    int n,i;
    scanf("%d\n",n);
    int arr[n];
    for(i=0;i<n;i++){
        scanf("%d ",&arr[i]); }
    return 0; }