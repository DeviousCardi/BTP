#include<stdio.h>
#include<stdlib.h>
int check=0;
int split(int arr[],int n) {
    for(int i=0;i<n;i++){
        for(j=0;j<n;j++){
            check } } }
int main() {
    scanf("%d",&n);
    int sum=0;
    int* arr;
    arr=(int*) malloc(n*sizeof(int));
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
        sum+=arr[i]; }
    return 0; }